# 当前任务状态

## 文件职责

`当前任务状态.md` 是新窗口最快理解当前情况的入口。

一句话职责：告诉新窗口“现在做到哪、下一步做什么、当前别踩什么坑”。

它只记录：

- 当前在做什么
- 已经完成什么
- 下一步最具体该做什么
- 当前风险和禁止事项
- 上一个窗口留下的必要交接

它不负责长期规则、阶段理论、产品功能解释或审计结论沉淀。

长期性结论不写在本文；本文只保留必要指针，指向对应主题记录或正式 `BK_Copilot/` 文档。

## 当前状态

当前阶段：

- Phase 1，材料盘点与系统地图

当前工作：

- 正在围绕 Entity / Entity Resolution / Case Log / Rule Match 边界做第一性原理讨论，并按用户确认结果瘦身 alias / write path / rule scope 相关设计。
- Entity 讨论是整个系统 audit 当前聚焦点，不是脱离系统审计的独立产品设计任务。
- 最新 Entity 讨论记录在 `Entity相关的所有问题.md`。

已完成：

- 旧审计宪章文档已改名为 `审计目标与原则.md`。
- 已新增 `项目背景速读.md`，作为新窗口快速理解 AI Bookkeeping 项目背景的短文档。
- 根部治理文档已中文化。
- Old system 材料已被明确定位为审计对象，不是历史意图权威、评判标准或 baseline。
- New system 材料已被明确定位为审计对象，不是正确性证明、当前 authority 或 implementation-ready contract。
- 已删除治理文档中主要会偷渡产品设计判断、成功标准或功能逻辑解释的内容。
- 已读取 `new system/new_system.md`。
- 已盘点 `new system/node_stage_designs/`：共有 15 个 node。
- 已将每个 node 的 Stage 1 / Stage 2 / Stage 3 合并为一份对应的 `*_node__design.md`。
- 合并文档保留定位、逻辑边界、contract 字段摘要和 Open Boundaries；删除阶段说明、examples、self-review、历史读档记录和重复解释。
- 已新增 `系统上下文地图.md`，提炼材料结构和来源边界，供新窗口快速建立上下文；New system / Old system 只作为审计对象。
- 已将最新标准 spec 模板规则放入 `BK_Copilot/node:layer spec template/`，并拆分为 workflow node 与 memory layer 两份模板规则文件。
- 已按 workflow node 标准模板新增 `BK_Copilot/workflow_nodes/entity_resolution_node/`，目前只覆盖 Stage 1-2，不创建 Stage 3 data contract。
- 已新增 `Entity相关的所有问题.md`，作为 Entity 讨论记录和后续问题入口。
- `TEMP_ENTITY_CASE_LOG_HANDOFF.md` 的内容已吸收进 `Entity相关的所有问题.md`。
- 已解决 Entity 未解决清单中的问题 2、问题 3 和问题 4。
- 已确认问题 5 不属于 Entity 本身，标注为后续处理 Coordinator 时处理。
- Entity / Alias 相关已确认结论已记录在 `Entity相关的所有问题.md`、`Alias question.md` 和 `BK_Copilot/` 对应正式草案中；本文不再复述长期结论正文。
- 已将 BK_Copilot 正式 Entity Log / Entity Resolution 文档对齐到最新 Entity 讨论。
- 已新增 `BK_Copilot/memory_layers/alias_log/`，作为 Alias Log 的正式 memory layer 草案入口，目前覆盖 M1 Memory Intent 和 M2 Authority / Lifecycle / Boundaries；字段、技术形态和写入路径未冻结。
- 已新增 `BK_Copilot/memory_layers/case_log/`，作为 Case Log 的正式 memory layer 草案入口，目前覆盖 M1 Memory Intent 和 M2 Authority / Lifecycle / Boundaries；核心来源限于 `BK_Copilot/` 已有正式草案 / 边界笔记、`Entity相关的所有问题.md` 和 `未解决问题暂存清单.md` 中已记录的 Entity / Case Log 相关问题。
- 已新增 `Rule Match临时文档.md`，记录当前 Rule Match / Rule Log 讨论中关于 rule scope、entity-level rule、pattern-level rule 和 Alias 边界的临时结论。
- 已将 `未解决问题暂存清单.md` 中的问题 1 收窄为 Alias 写入问题；automation policy 不再放在问题 1 中。
- 已将 AI 联网搜索、unknown identity runtime context、Rule Match 已确认边界和 `new_stable_entity` 最小创建 provenance 同步进 `BK_Copilot/` 下 Entity Resolution / Entity Log 正式草案。
- 已新增 `Entity key question.md`，短记 stable entity 判断 / 自动 stable 边界，以及 candidate identity signal / candidate-linked case 的表达、持久化和升级问题。
- 已新增 `Candidate Entity question.md`，记录 Candidate Identity / Candidate Entity 的已确认结论和仍未冻结边界。
- 已根据用户确认关闭 `未解决问题暂存清单.md` 中的 Role 字段问题；Role 字段目前已删除，不再作为 Entity Log 正式字段候选处理。
- 单独决策文档已删除；后续不再维护单独决策文件，已确认结论写入用户指定的长期结论文档、对应主题记录、正式 `BK_Copilot/` 文档或必要的交接记录。

## 当前目标

让治理文档保持克制：

- 新窗口能快速接续。
- 上下文满时知道如何交接。
- 规则文档不替 agent 解释当前系统功能逻辑。
- 规则文档不预设节点、字段、流程、抽象或拆分是否合理。

## 材料角色

- `AGENTS.md`：操作入口和交接纪律。
- `审计目标与原则.md`：稳定审计目的与立场约束。
- `项目背景速读.md`：项目背景速读。
- `系统上下文地图.md`：材料结构、来源边界和审计对象位置的高密度上下文速读。
- `审计阶段路线图.md`：轻量阶段导航。
- `当前任务状态.md`：当前状态和下一步。
- `new system/`：New system 材料，只作为审计对象，不具有权威参考意义。
- `BK_Copilot/node:layer spec template/workflow_node_spec_template_rules.md`：后续最新标准 workflow node spec 的模板规则。
- `BK_Copilot/node:layer spec template/memory_layer_spec_template_rules.md`：后续最新标准 memory layer spec 的模板规则。
- `BK_Copilot/workflow_nodes/entity_resolution_node/`：Entity Resolution Node 的当前标准草案，覆盖 Functional Intent 与 Logic and Boundaries。
- `BK_Copilot/memory_layers/alias_log/`：Alias Log 的当前标准草案，覆盖 M1-M2；核心来源为 `Alias question.md`。
- `BK_Copilot/memory_layers/case_log/`：Case Log 的当前标准草案，覆盖 M1-M2；不冻结 data contract、写入者或多 log finalization 机制。
- `Entity相关的所有问题.md`：Entity 讨论的当前临时记录；若内容已在该文档中，不要重复塞进治理文档。
- `Entity key question.md`：Entity 相关 node / log 当前仍需处理的问题速记；当前包含 stable entity 判断 / 自动 stable 边界，以及 candidate identity signal / candidate-linked case 的表达、持久化和升级问题。
- `Candidate Entity question.md`：Candidate Identity / Candidate Entity 的已确认结论入口；不冻结 data contract、storage implementation 或 writer contract。
- `Alias question.md`：保留为 Alias 主题速记；其内容已同步进 `Entity相关的所有问题.md` 和正式 Entity Log / Entity Resolution 草案，不作为更高 authority。
- `Rule Match临时文档.md`：Rule Match / Rule Log 当前临时结论，记录 rule scope 和 Alias 在 Rule Match 中的位置。
- 用户后续创建的长期结论文档：长期性结论的承载位置；规则文档不承载这些结论正文。
- `old_system_nodedesign/`：Old system 材料，只作为审计对象，不具有权威参考意义。

## 下一步

下一轮审计应：

1. 下一步继续解决 `Entity key question.md` 中问题 B 剩余未冻结项：Case Judgment eligibility、candidate identity exact schema / writer、promotion workflow、candidate-linked case upgrade 和多 log 写入机制。
2. 开始前先读取 `Candidate Entity question.md`、`Entity key question.md`、`Entity相关的所有问题.md`、`BK_Copilot/memory_layers/entity_log/`、`BK_Copilot/memory_layers/case_log/` 和 `BK_Copilot/workflow_nodes/entity_resolution_node/`。
3. 本轮先不要继续 Alias 写入问题、同 batch unknown entity 去重或 Coordinator 问题，除非用户明确切换任务。
4. 继续使用一问一答方式推进；形成确认性结论后先复述给用户确认。
5. 问题 B 解决后，需要更新 `Entity key question.md`、`Entity相关的所有问题.md`、`BK_Copilot/` 中受影响的正式边界文档、`当前任务状态.md` 和 `未解决问题暂存清单.md`。
6. 始终把 Entity 讨论锚定为系统 audit 的一部分，用核心产品目标评估必要性、边界和复杂度。

## 当前风险

- 治理文档可能再次膨胀成产品设计指南。
- `审计阶段路线图.md` 容易滑向定义审计成功标准。
- `审计目标与原则.md` 容易滑向解释系统应该如何设计。
- 合并文档是瘦身摘要，不再逐字保留所有原 Stage 文档解释性文本；如需追溯旧全文，应通过 git 历史查看被删除的 Stage 文件。
- Entity 讨论仍有未决问题，不能把临时记录当成正式 spec 或冻结 data contract。

## 禁止事项

- 不要在规则文档中加入产品功能逻辑细节。
- 不要在规则文档中定义节点、字段、流程或抽象的好坏标准。
- 不要把 Old system 当评判标准、历史意图权威或 baseline。
- 不要把 New system 当正确答案、当前 authority 或 implementation-ready contract。
- 不要把 `new system/` 或 `old_system_nodedesign/` 中的任何信息当作权威参考；它们都是审计对象。
- 不要在未获得用户明确要求时编辑 `new system/` 或 `old_system_nodedesign/`。
- 不要重新生成 45 份 Stage 文档，除非用户明确要求。
- 不要把未确认的 Entity 判断写入正式 `BK_Copilot/` 文档。
- `未解决问题暂存清单.md` 只记录用户明确同意写入的问题，不得自行添加。
- 不要把 `Entity相关的所有问题.md` 中已有内容重复长篇搬进治理文档。

## 最近交接
- Entity / Alias 结论正文不要从本节读取，去读 `Entity相关的所有问题.md`、`Alias question.md` 和 `BK_Copilot/` 对应正式草案。
- Alias Log 已有独立正式草案入口：`BK_Copilot/memory_layers/alias_log/`。当前只冻结 M1-M2，不包含 data contract、storage 或 Rule Match 支持。
- Case Log 已有独立正式草案入口：`BK_Copilot/memory_layers/case_log/`。当前只冻结 M1-M2，不包含 data contract、writer contract 或多 log finalization 机制。
- Rule Match 当前临时结论入口：`Rule Match临时文档.md`。重点包括 rule scope、entity-level rule、pattern-level rule 和 Alias 的 runtime 位置。
- Entity 未解决清单中的问题 2、问题 3、问题 4 已解决；问题 5 延后到 Coordinator 审计时处理。
- 当前下一步是继续解决 `Entity key question.md` 的问题 B 剩余未冻结项；Candidate Identity 已确认结论入口为 `Candidate Entity question.md`。
- `Entity key question.md` 是 stable entity 判断 / 自动 stable 边界，以及 candidate identity signal / candidate-linked case 表达、持久化和升级问题的新入口。
- Role 字段问题已关闭；Role 目前已删除，不再作为 Entity Log 正式字段候选处理。
- Alias 写入问题、问题 6（同 batch unknown entity 去重）和 Coordinator 问题暂不作为下一步，等待用户后续切换回来。
