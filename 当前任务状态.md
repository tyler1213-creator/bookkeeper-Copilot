# 当前任务状态

## 文件职责

`当前任务状态.md` 是新窗口最快理解当前情况的入口。

一句话职责：告诉新窗口“现在做到哪、下一步做什么、当前别踩什么坑”。

它只记录：

- 当前在做什么
- 已经完成什么
- 下一步最具体该做什么
- 当前风险和禁止事项
- 上一个窗口留下的必要交接

它不负责长期规则、阶段理论、产品功能解释或审计结论沉淀。

长期性结论不写在本文；本文只保留必要指针，指向对应主题记录或正式 `BK_Copilot/` 文档。

## 当前状态

当前阶段：

- Phase 1，材料盘点与系统地图。

当前工作：

- Entity 圈 `Entity Resolution -> Alias Log -> Entity Log -> Case Log -> Rule Match -> Rule Log` 六个对象的 L2 / M1-M2 已落地到 `BK_Copilot/` 正式草案。
- Evidence Intake L2 提案已形成，正式 `BK_Copilot/workflow_nodes/evidence_intake_node/` 草案尚未创建；剩余字段 / seam / DEFERRED 项已迁入 `缺口地图.md` 的 Evidence Intake section。
- `_work/` 临时目录已删除；其中仍有价值的内容已迁入 `缺口地图.md`、`L2_proposals/Evidence Intake__L2提案.md` 与根目录 `interaction_agent.md`。
- 本轮治理文档瘦身已完成：规则 / 上下文文档只保留读档路线、材料导航、当前任务和未冻结缺口，不再复述正式草案或 L2 提案里的已收口结论。
- `缺口地图.md` 后续作为跨节点缺口池使用；已收口 L1 / L2 不再写入，L3 / L4 / seam / L2·外阻继续保留，后续节点审计发现的新缺口继续追加。
- 已审或讨论中的对象入口是 `L2_proposals/[对象]__L2提案.md`；正式文档最新信息以 `BK_Copilot/` 对应草案为准。
- `Entity相关的所有问题.md` 已归档，不再更新。
- `未解决问题暂存清单.md` 已废弃，只作为历史参考，不再作为当前问题入口。

## 当前目标

让治理文档保持克制：

- 新窗口能快速接续。
- 上下文满时知道如何交接。
- 规则文档不替 agent 解释当前系统功能逻辑。
- 规则文档不预设节点、字段、流程、抽象或拆分是否合理。
- 已收口设计事实回到 `BK_Copilot/` 正式草案。
- 未冻结问题集中进入 `缺口地图.md` 或对应对象的正式 Open Boundary。

## 材料角色

- `AGENTS.md`：操作入口、权威顺序和交接纪律。
- `当前任务状态.md`：当前状态、下一步和 active risks。
- `审计目标与原则.md`：稳定审计目的与立场约束。
- `审计阶段路线图.md`：轻量阶段导航。
- `项目背景速读.md`：项目背景速读。
- `系统上下文地图.md`：材料结构、来源边界和审计对象位置的导航。
- `缺口地图.md`：尚未收口的 L3 / L4 / seam / L2·外阻缺口池；不记录已收口 L1 / L2。
- `BK_Copilot/`：已经开始重写的正式 node / memory layer 草案；当前标准边界以这里为准。
- `BK_Copilot/node:layer spec template/`：后续写正式 node / memory layer spec 的模板规则。
- `L2_proposals/`：已审定或讨论中的 L2 提案入口；不替代 `BK_Copilot/` 正式草案。
- `interaction_agent.md`：交互 / 目标层 Agent 的圈外概念入口，含覆盖完整性核对职责；正式 spec 未冻结。
- `new system/`：New system 材料，只作为审计对象，不具有权威参考意义。
- `old_system_nodedesign/`：Old system 材料，只作为审计对象，不具有权威参考意义。
- 主题速记如 `Alias question.md`、`Rule Match临时文档.md`、`Coordinator Question.md`：只作为历史或待正式审计对象的讨论入口，不高于正式草案。

## 下一步

下一轮审计应：

1. 如果正式落 Evidence Intake 01/02，先读 `L2_proposals/Evidence Intake__L2提案.md`、`缺口地图.md` 的 Evidence Intake section、`BK_Copilot/node:layer spec template/workflow_node_spec_template_rules.md`。
2. 如果继续审计圈外对象（interaction_agent、Governance、Case Judgment、Knowledge Summary、Coordinator 等），读取 `缺口地图.md` 中相关 L2·外阻 / seam 项，并将新发现的未冻结缺口追加到对应 section。
3. 如果做 Entity 圈接缝审，优先读取 `BK_Copilot/` 下六个正式草案目录，再读取 `缺口地图.md` 中保留的未冻结项。
4. 不要把已收口 L1 / L2 重新搬回 `缺口地图.md` 或新建临时工作目录；只保留仍需要后续 L2、L3、L4 或 seam 决策的问题。

## 当前风险

- 治理文档可能再次膨胀成产品设计指南。
- `缺口地图.md` 容易重新变成“已完成结论清单”，应只保留未冻结缺口。
- `系统上下文地图.md` 容易复述正式草案边界，应只做材料导航。
- `当前任务状态.md` 容易变成历史流水账，应只保留当前交接。
- 根目录主题文档容易变成长期 shadow authority；正式落文后应迁入 `BK_Copilot/` 草案或 `缺口地图.md` Open Boundary。
- `审计阶段路线图.md` 容易滑向定义审计成功标准。
- `审计目标与原则.md` 容易滑向解释系统应该如何设计。

## 禁止事项

- 不要在规则文档中加入产品功能逻辑细节。
- 不要在规则文档中定义节点、字段、流程或抽象的好坏标准。
- 不要把 Old system 当评判标准、历史意图权威或 baseline。
- 不要把 New system 当正确答案、当前 authority 或 implementation-ready contract。
- 不要把 `new system/` 或 `old_system_nodedesign/` 中的任何信息当作权威参考；它们都是审计对象。
- 不要在未获得用户明确要求时编辑 `new system/` 或 `old_system_nodedesign/`。
- 不要继续更新 `Entity相关的所有问题.md`；后续 Entity 讨论问题写入 `L2_proposals/[对象]__L2提案.md`，正式文档最新信息仍看 `BK_Copilot/`。
- 不要继续更新或使用 `未解决问题暂存清单.md` 作为当前待办入口；该文件已废弃，只能作为历史参考。
- 不要恢复 `_work/` 作为长期信息承载地；临时材料完成整理后必须迁入对应正式入口并删除。

## 最近交接

- 本轮清理目标：瘦身 `当前任务状态.md`、`系统上下文地图.md`、`缺口地图.md`。
- `缺口地图.md` 的当前口径：清理所有已确认且已收口的 L1 / L2；L3、L4、seam 和 L2·外阻保留，供后续节点继续追加缺口。
- `系统上下文地图.md` 的当前口径：只做材料导航，不复述各正式草案已冻结的设计结论。
- `当前任务状态.md` 的当前口径：只做当前交接，不承载已完成对象的详细结论。
- 2026-06-12 Evidence Intake / Transaction Identity 讨论轮：Transaction Identity 节点已删除（用户指示 Codex 直接执行），transaction_id 铸号并入 Evidence Intake 流程第 9 步，跨导入复用 / 迟到证据接回 / durable registry 全部 DEFERRED。
- 触及已锁正式文档留痕：因删除 TI，已改动正式文档 BK_Copilot/workflow_nodes/entity_resolution_node/01_functional_intent.md 与 02_logic_and_boundaries.md 中 transaction_id 的来源（Transaction Identity Node → Evidence Intake）。此为触及已锁正式草案的改动，经用户授权执行。
- pending concepts 重组：balance_chain / evidence_pool / correction_path 并入 L2_proposals/Evidence Intake__L2提案.md；coverage_completeness_check 并入 interaction_agent.md；现仅保留根目录 interaction_agent.md 一份独立概念。
- EI/TI 讨论入口与最新状态：L2_proposals/Evidence Intake__L2提案.md；剩余 L3 / L4 / seam / DEFERRED 项看 缺口地图.md 的 Evidence Intake section；覆盖完整性目标层职责看 interaction_agent.md。
- 写正式 EI 01/02 文档时的防漏纪律：逐条对照提案的"拟改:文件:章节"行落字，并回勾 缺口地图.md 的 Evidence Intake section；依赖 01/02 模板的"输出类别/契约面总表"强制点名每一个对外输出（含 duplicate_material_candidate、实际覆盖信号、EvidenceAssociationResult 等），不靠记忆。
- 本轮临时目录清理：_work/ 已删除；不要再从临时工作目录读取 EI 状态或缺口，入口已归位到 EI L2 提案、缺口地图和 interaction_agent.md。
