# 当前任务状态

## 文件职责

`当前任务状态.md` 是新窗口最快理解当前情况的入口。

一句话职责：告诉新窗口“现在做到哪、下一步做什么、当前别踩什么坑”。

它只记录当前状态、下一步、active risks 和必要交接；长期规则看 `AGENTS.md`，稳定原则看 `审计目标与原则.md`，材料导航看 `系统上下文地图.md`，未冻结缺口看 `缺口地图.md`，正式边界看 `BK_Copilot/`。

## 当前任务状态

当前阶段仍是 Phase 1：材料盘点与系统地图。

已经落入 `BK_Copilot/` 的正式草案：

- Workflow node：`Evidence Intake`、`Entity Resolution`、`Rule Match`、`Case Judgment`、`Coordinator`、`Human Review Node`，均为 draft，已覆盖 Stage 1-2，尚未进入 Stage 3 data contract。
- Memory layer：`Alias Log`、`Entity Log`、`Case Log`、`Rule Log`、`Transaction Log`，均为 draft，已覆盖 M1-M2，尚未进入 M3 data contract。
- Shared mechanism：`Finalization 写入机制` 已落入 `BK_Copilot/shared_mechanisms/finalization/` 正式 draft（建模式，仅 `00_index.md` / `01_functional_intent.md` / `02_logic_and_boundaries.md`），覆盖 Stage 1-2，尚未进入 Stage 3 data contract。
- `Case Log` 另有 `case_log_boundary_note.md`，状态为 superseded boundary note，只作为正式 M1-M2 草案来源之一，不是当前完整 spec。

当前正式文档状态：

- 已审对象的 L1 / L2 结论应以 `BK_Copilot/` 对应正式草案为准。
- L3 / L4 / seam / L2·外阻问题集中在 `缺口地图.md`，不要回灌到规则文档。
- `Coordinator` 已根据 `L2_proposals/Coordinator__L2提案.md`（决策 1–15 + F1）和用户澄清落入 `BK_Copilot/workflow_nodes/coordinator_node/` 正式 draft，覆盖 Stage 1-2；原三项 Stage 3 前置中，`Transaction Log` 已落为正式 M1-M2 draft，仍待 JE Generation / Intervention Log 落文 + L3/L4 单独推进。`缺口地图.md` Coordinator section 由用户单独维护，本轮不作为当前状态更新对象。
- `Human Review Node` 已根据 `L2_proposals/HumanReviewNode__L2提案.md` 和用户 2026-06-22 澄清落入 `BK_Copilot/workflow_nodes/human_review_node/` 正式 draft（建模式：`00_index.md`、`01_functional_intent.md`、`02_logic_and_boundaries.md`），覆盖 Stage 1-2；未冻结 L3/L4/seam/L2·外阻继续归 `缺口地图.md` 的 Human Review Node section。
- `Finalization 写入机制` 已根据 `L2_proposals/Finalization__L2提案.md` 和用户 2026-06-23 澄清落入 `BK_Copilot/shared_mechanisms/finalization/` 正式 draft（建模式：`00_index.md`、`01_functional_intent.md`、`02_logic_and_boundaries.md`），覆盖 Stage 1-2；本对象是第三类跨层共享机制，非 workflow node、非 memory layer；未冻结 L3/L4/seam/L2·外阻继续归 `缺口地图.md` 的 Finalization section。
- `interaction_agent.md` 仍是根目录概念入口，尚未形成正式 `BK_Copilot/` spec。
- Knowledge Summary、Governance、Profile、Evidence Log、JE Generator、Intervention Log 等仍是圈外或未正式落文对象，只能通过 `缺口地图.md` 和相关主题入口追踪接缝；Case Judgment、Coordinator 和 Human Review Node 已落入正式 draft，但都仍依赖这些圈外对象的 open boundary。
- `Transaction Log` 已从 `L2_proposals/Transaction Log__L2提案.md`（决策 1–9 + 用户澄清）落入 `BK_Copilot/memory_layers/transaction_log/` 正式 M1-M2 draft（建模式：`00_index.md`、`01_memory_intent.md`、`02_authority_lifecycle_and_boundaries.md`）。未冻结 L3/L4/seam/L2·外阻 继续归 `缺口地图.md` 的 Transaction Log section；进 M3 卡 L3 字段定稿 + 圈外依赖（JE Generation / Intervention Log / Review / Case Memory Update / 对外 output report）落文。

## 规则文档现状

根目录 active 规则 / 上下文文档只保留：

- `AGENTS.md`
- `当前任务状态.md`
- `审计目标与原则.md`
- `审计阶段路线图.md`
- `项目背景速读.md`
- `系统上下文地图.md`
- `缺口地图.md`

当前口径：

- `AGENTS.md` 只负责 agent 操作规则、source authority、必读材料、可编辑范围、停止条件和输出纪律。
- `当前任务状态.md` 只负责实时交接，不沉淀已收口设计结论。
- `系统上下文地图.md` 只做材料导航，不复述正式草案的产品边界。
- `缺口地图.md` 只记录未冻结 L3 / L4 / seam / L2·外阻缺口，不记录已收口 L1 / L2。
- `未解决问题暂存清单.md` 已废弃，只能作为历史参考，不再作为当前待办入口。

## 下一步

下一轮应优先选择一个明确方向：

1. 继续 Evidence Intake 的 L3 / L4 / seam：先读 `BK_Copilot/workflow_nodes/evidence_intake_node/`，再读 `缺口地图.md` 的 Evidence Intake section。
2. 推进圈外对象正式落文：优先从 Knowledge Summary、Governance、Profile、Evidence Log、JE Generator、Intervention Log 等当前接缝阻塞项中选一个；Coordinator 进 Stage 3 的原三项前置中，`Transaction Log` 已完成正式落文，仍待 JE Generation / Intervention Log。
3. 做 Entity 圈接缝审：先读 `BK_Copilot/` 下六个相关正式草案目录，再读 `缺口地图.md` 中 Entity / Alias / Case / Rule 相关未冻结项。
4. 推进 Coordinator 的 Stage 3 前置：先补 JE Generation / Intervention Log 这两个剩余圈外下游之一，再回到 `BK_Copilot/workflow_nodes/coordinator_node/00_index.md` 的 gate 检查。

做正式 node / memory layer 文档前，必须同时读取：

- 对应 `BK_Copilot/` 草案
- `BK_Copilot/node:layer spec template/` 下对应模板规则
- `缺口地图.md` 中该对象的未冻结项
- 必要时读取 `L2_proposals/` 作为追溯入口

## 当前风险

- 规则文档重新膨胀成产品设计指南。
- 当前状态重新变成历史流水账。
- 系统地图复述正式草案边界，形成 shadow authority。
- 缺口地图重新混入已收口 L1 / L2 结论。
- 根目录概念文档在正式落文后继续充当长期 authority。
- New system / Old system 被误当成 baseline、正确答案或历史意图权威。
- 新窗口误以为 Governance Review / Post-Batch Lint / 旧 Review Node 仍作为独立节点存在（已于 2026-06-21 废除或改名，见最近重要交接）。
- 旧三套机制交接文档被误读：`Finalization_question.md` / Human Review 旧 question 材料已被正式 draft 替代，只作历史背景；但 `独立question文档/Deterministic_Discovery_question.md` 已升为确定性发现机制当前唯一权威设计文档（pending 正式 spec），不能再当普通历史背景。

## 禁止事项

- 不要在规则文档中加入产品功能逻辑细节。
- 不要把已收口 L1 / L2 重新搬回 `缺口地图.md`。
- 不要把 `new system/` 或 `old_system_nodedesign/` 当权威参考。
- 不要在未获得用户明确要求时编辑 `new system/` 或 `old_system_nodedesign/`。
- 不要继续更新或使用 `未解决问题暂存清单.md` 作为当前待办入口。

## 最近重要交接

- 2026-06-23 系统自发发现层已裁撤删除：
  - 用户拍板删除整个「系统自发语义发现层」= 语义发现器（系统自发 merge / split）+ FP-5（系统自发发现错误）。二者本质同一问题（「系统自发语义判断 → 喂给人审」），一起删。
  - 理由：能审计的部分已是确定性发现 / 写入闸 / ER 运行期判句（不需要语义节点）；需要语义的部分依据非法（Alias Log §8 已拒绝 semantic similarity 作 identity authority），与「rule 失效归人」同属人专属先例。
  - 保留不动：确定性发现 job（rule 升级候选）、人发起 merge / split（Human Review）、Alias / Entity / Finalization 中 merge / split 的变更语义与 `merge_split_candidate` 候选类型（其生产者收窄为确定性冲突判句 + 人发起，不再有系统自发语义生产者）。
  - 已改（裁决落文）：HRN `00/01/02` 三份正式草案、`缺口地图.md`（顶部废除节点说明 + HRN section L2·外阻收口）、本文件、`独立question文档/Deterministic_Discovery_question.md` 与 `Human_Review_Node_question.md` 的相关排除项、项目记忆（删除 `semantic-discovery-node-necessity-open`、更新 `governance-layer-node-mechanism-division`）。
  - 已改（废除 / 删除节点引用清理，正式文档全扫）：`Entity Log 02`（读取者 / 写入者 / lifecycle / 冲突 / mutation path——Post-Batch Lint + Governance Review Node 读取行删除，merge/split/policy 写入改为「会计师人发起经 Human Review 备料 → Finalization 落盘」，auto-downgrade 归 automation_policy maintenance，旧 Review Node → Human Review Node）、`Coordinator 00/01/02`、`ER 01/02`、`Case Log 02`、`Transaction Log 00/02`。全部 `BK_Copilot/` 已无 Post-Batch Lint / Governance Review Node / 旧 Review Node / 语义发现器 的 live 引用（已扫描确认）。
  - 未改（历史记录，按现行 authority 文档为准）：`L2_proposals/`、`_audit_work/`、`Case Log case_log_boundary_note.md`（superseded boundary note）中记录当时状态的追溯材料；正式草案中与废除节点无关的「旧 candidate→指定节点裸写」writer 模型泛化清理仍属 A 类残留，按单独对象 prompt 推进。
- 2026-06-23 确定性发现机制已升为当前唯一权威设计文档：
  - `独立question文档/Deterministic_Discovery_question.md` 已按用户审定结论更新为确定性发现机制当前唯一权威设计文档（pending 未来正式 `BK_Copilot/` shared mechanism spec）；它仍不是字段级 contract，不表示任何 Stage / M 阶段完成。
  - 身份已锁：确定性发现是挂在调度器上的独立通用非 LLM 笨 job，属第三类跨层共享机制；不折进 Human Review、Rule Match 或 Rule Log。seam：promotion 资格判句归 Rule 侧治理，扫描 / 调度 / 写审核 inbox 归机制管道。
  - 职责已锁：只发现确定性可发现的扩张型变更；今天唯一客户 = Rule 升级发现侧，唯一候选 = `rule_promotion_candidate`。Rule 升级固定执行路径归 Rule 侧定义，job 只写候选，Human Review 只确认并触发。
  - 排除项已锁：rule 降级 / 失效判断归人，本机制不产降级候选、不累积“被推翻”信号；automation 收紧归 Entity Log `automation_policy` maintenance / mutation contract；语义型 merge / split 的**系统自发发现已裁撤删除**（只保留人发起）；跨 rule 互斥 / 引用校验是写入 / 生效前同步守卫。
  - Rule Log / Rule Match 正式草案中 promotion 发现侧 open boundary 已加指针指向该文档；Rule 侧仍拥有 promotion 资格判句定义权，确定性发现只套用。
- 2026-06-23 Finalization 写入机制已落正式 draft：
  - `L2_proposals/Finalization__L2提案.md` + 用户 2026-06-23 澄清已转入 `BK_Copilot/shared_mechanisms/finalization/`（建模式，仅 `00_index.md` / `01_functional_intent.md` / `02_logic_and_boundaries.md`），覆盖 Stage 1-2。
  - 本对象是第三类跨层共享机制，非 workflow node、非 memory layer；采用 node 模板 00/01/02 骨架 + 四处改写（调用关系、写入反转、动作集 / 凭证闸门 / 回执失败、反向 seam），无专用模板。
  - `独立question文档/Finalization_question.md` 降为历史背景，不再作为正式 contract 来源。
  - 未冻结项保留在 `缺口地图.md` Finalization section：L3 动作 / 凭证 / 回执 / ID schema，L4 多 log 原子 / 回滚 / 幂等 / 同步可见性，L2·外阻 Governance Log / 审核 inbox / Intervention Log / merge-split 迁移契约 / 目录命名确认等。**勿当冻结契约。**
- 2026-06-22 Human Review Node 已落正式 draft：
  - `L2_proposals/HumanReviewNode__L2提案.md` + 用户 2026-06-22 澄清已转入 `BK_Copilot/workflow_nodes/human_review_node/`（建模式，仅 `00_index.md` / `01_functional_intent.md` / `02_logic_and_boundaries.md`），覆盖 Stage 1-2；旧 question 材料只作历史背景，不再作为正式 contract 来源。
  - 未冻结项保留在 `缺口地图.md` Human Review Node section：L3 字段 / schema、Finalization 与 Change List Engine seam、Intervention Log / Governance Log / 审核 inbox / 确定性发现 / NEW-1 / C2 / A 类残留待写回等 open boundary。**勿当冻结契约。**（系统自发发现层已于 2026-06-23 裁撤删除，不再是 open boundary。）
- 2026-06-22 三套机制讨论轮（历史交接；其中确定性发现已于 2026-06-23 升为当前唯一权威设计文档，Finalization / Human Review 已落正式 draft）：
  - 三套机制最终去处厘清并落交接文档到 `独立question文档/`：`Deterministic_Discovery_question.md`（确定性发现，后续已升当前唯一权威设计文档，pending 正式 spec）、`Finalization_question.md`（finalization 写入，后续已落 `BK_Copilot/shared_mechanisms/finalization/` 正式 draft，question 文档降为历史背景）；授权确认的人类确认面后续已随 Human Review Node 落入 `BK_Copilot/workflow_nodes/human_review_node/` 正式 draft，凭证强制半边归 Finalization。
  - **架构净变化**：「三套待打磨机制」→ 实为 **两套机制（确定性发现 job + Finalization 库）+ 一个统一人类确认面（Human Review Node）**。**授权确认机制不再独立**：UX / 确认 / 调度半边并入 Human Review Node；凭证强制半边并入 Finalization。
  - 三套**互相独立、非脊柱**：共用 Finalization 这块落盘地板，但确定性发现 / 授权确认各自只在部分路径出现——ER→Entity Log 仅走 Finalization 免闸；restrictive auto-downgrade 免闸；运行期 / 候选写入免凭证；扩张型变更才过 Human Review 确认 + 带凭证落盘。
  - **确定性发现** = 自主笨 job（非 LLM、调度器触发、双节奏：批后增量 + 定时全扫；产候选投审核 inbox；Review 拉取消费、job 永不 call Review）。互斥 / 引用校验**不归它**（写入前同步守卫）。语义发现器已于 2026-06-23 裁撤删除。
  - 后续整合收窄：确定性发现当前只服务 Rule Match / Rule 侧 promotion，当前 inbox 候选 = `rule_promotion_candidate`；promotion 资格判句归 Rule 侧，job 只套用；automation auto-downgrade 的检测 / 自动收紧 / 治理可见性改归 Entity Log `automation_policy` maintenance / mutation contract，不归当前确定性发现 job。审核 inbox 草案见 `独立question文档/Review_Inbox_question.md`。
  - **配套新数据层**：Governance Log（权威变更审计账本）、审核 inbox（候选队列），均待建。
  - Finalization L1-L2 已在 2026-06-23 落正式 draft；仍未冻结：finalization 多 log 原子 / 顺序 / 幂等 L4、凭证 exact 形态、inbox 候选状态 / 去重 / 过期（MVP 搁置）。**勿当冻结契约。**
  - 同步更新 `系统上下文地图.md`（材料地图 + 尚未落文清单）与 `缺口地图.md`（顶部「跨层共享机制」指针 + 废除节点改读说明）。后续整合本轮另补了 Rule Log / Rule Match / Entity Log 的 open boundary 指针，未改 Human Review Node 正式草案。
- 2026-06-21 规则治理层重构（历史设计结论；Human Review 相关部分后续已落正式 draft，剩余共享机制 / 数据层仍待落文）：
  - **废除**独立节点 **Governance Review**（其审批 → 授权确认机制：人+确定性代码、无 LLM 推理）、**Post-Batch Lint**（拆为 确定性发现 job + 语义发现器；后者已于 2026-06-23 裁撤删除）。
  - 旧 **Review Node** 宽泛职责拆散：当前交易交互→Coordinator、审批→授权确认机制、展示+纠错→新节点、留痕→Intervention Log 接缝。
  - 新节点 = **Human Review Node**（旧称 Review Node，本轮改名）= 复核视图 + Change List LLM + Change List Engine + 对账仲裁。
  - 本轮内部改判：「授权闸」**非节点、是共享机制**（授权确认机制）；「影响展开引擎」收进 Human Review Node 内（Change List Engine），对账+仲裁是节点内步骤。
  - 否决：节点裸写 log / 加 LLM 写手（→ 节点提 payload、确定性 finalization 写入机制落盘）；LLM 当场裁决 rule 坏没坏。后续已收口为：rule 失效 / 降级归人，确定性发现不产降级候选、不累积“被推翻”信号。
  - **语义发现器**（系统自发 merge/split）：当时挂起，已于 2026-06-23 裁撤删除（连同 FP-5 系统自发错误发现）。
  - 三个待打磨机制的旧表述已被后续更新覆盖：Finalization 已落正式 draft；授权确认已分解进 Human Review + Finalization；确定性发现已升当前唯一权威设计文档但仍 pending 正式 spec。共用新数据层 **Governance Log**（缺件待建）。
  - 未动：Coordinator / ER / CJ / Rule Match、五个 memory layer。
- 2026-06-18 Transaction Log 已按用户审定的 L2 提案与澄清落入 `BK_Copilot/memory_layers/transaction_log/` 正式 M1-M2 draft（建模式，仅 00/01/02）；其余字段、机制、圈外依赖仍在 `缺口地图.md` Transaction Log section 追踪。Coordinator Stage 3 原三项前置中，Transaction Log 已完成，剩 JE Generation / Intervention Log。
- 2026-06-18 Transaction Log L2 提案复审（按「L2 审计 Prompt 模板」，建模式）通过：决策 1–9 全部成立、守 seam/L3/圈外纪律、无禁止写法。用户补 3 项拍板并已折入提案——①情况 Y 身份缺口标记落点锁定为 Transaction Log（A 原为「初步倾向/未冻结」）；②Transaction Log 无 candidate 层、写入即 finalization 定稿；③记录无状态机、写下即固定（finalized/terminal/blocked 为内容字段、enum 留 L3）。同步编辑了 `BK_Copilot/memory_layers/case_log/01_memory_intent.md:127` 与 `02_authority_lifecycle_and_boundaries.md:110`，把「身份缺口标记落点：初步倾向/未冻结」改为「已定为 Transaction Log」（其余 trigger order/writer/finalization 机制仍标未冻结，未碰）。该提案已在同日转正式 draft。
- 2026-06-18 Transaction Log 完成第一性原理 L1-L2 审计（按「新节点审计 Prompt 模板」）：确认为 **memory layer**，真值单位 = 逐笔交易，定位为 transaction-indexed 逐笔交易全过程审计 source of truth。L2 结论落入 `L2_proposals/Transaction Log__L2提案.md`；L3/L4/seam/L2·外阻/DEFERRED 归入 `缺口地图.md` Transaction Log section。读档底稿在 `_audit_work/transaction_log/`，后续已转正式 `BK_Copilot/memory_layers/transaction_log/` draft。
- 2026-06-18 Coordinator 节点已从 `L2_proposals/Coordinator__L2提案.md` 落入 `BK_Copilot/workflow_nodes/coordinator_node/` 正式 draft，覆盖 Stage 1-2；原 Stage 3 三项前置中 Transaction Log 已另落正式 M1-M2 draft，仍待 JE Generation / Intervention Log 落文 + L3/L4 单独推进。`缺口地图.md` Coordinator section 由用户单独维护，本轮未触碰。
- 2026-06-18 Case Judgment 已根据 `L2_proposals/Case Judgment__L2提案.md` 落入 `BK_Copilot/workflow_nodes/case_judgment_node/` 正式 draft，覆盖 Stage 1-2；剩余 L3 / L4 / seam / L2·外阻已归位到 `缺口地图.md`。
- 2026-06-12 Evidence Intake / Transaction Identity 讨论轮：Transaction Identity 节点已删除，`transaction_id` 铸号并入 Evidence Intake 流程第 9 步，跨导入复用 / 迟到证据接回 / durable registry 全部 DEFERRED。
- 因删除 Transaction Identity，已按用户授权改动 `BK_Copilot/workflow_nodes/entity_resolution_node/01_functional_intent.md` 与 `02_logic_and_boundaries.md` 中 `transaction_id` 来源。
- pending concepts 已归位：`balance_chain`、`evidence_pool`、`correction_path` 并入 `L2_proposals/Evidence Intake__L2提案.md`；`coverage_completeness_check` 并入 `interaction_agent.md`。
- 临时 `_work/` 已删除；Evidence Intake 最新入口是正式草案、Evidence Intake L2 提案、`缺口地图.md` 和 `interaction_agent.md`。
