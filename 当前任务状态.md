# 当前任务状态

## 文件职责

`当前任务状态.md` 是新窗口最快理解当前情况的入口。

一句话职责：告诉新窗口“现在做到哪、下一步做什么、当前别踩什么坑”。

它只记录：

- 当前在做什么
- 已经完成什么
- 下一步最具体该做什么
- 当前风险和禁止事项
- 上一个窗口留下的必要交接

它不负责长期规则、阶段理论、产品功能解释或审计结论沉淀。

长期性结论不写在本文；本文只保留必要指针，指向对应主题记录或正式 `BK_Copilot/` 文档。

## 当前状态

当前阶段：

- Phase 1，材料盘点与系统地图

当前工作：

- 正在围绕 Entity 圈按 `ER → Alias Log → Entity Log → Case Log → Rule Match → Rule Log` 顺序推进 L2 提案。
- ER、Alias Log、Entity Log、Case Log 已完成 L2 提案；RuleMatchNode 节点侧 L1-L2 已实质完成（见 `L2_proposals/Rule Match__L2提案.md`）；下一步开 **Rule Log L1-L2**——RuleMatch 收尾时把 RuleLog 顶到台前（资格判据归属、entity_id 索引、automation_policy 取值语义、rule lifecycle 都在 RuleLog 落地）。
- Entity 讨论是整个系统 audit 当前聚焦点，不是脱离系统审计的独立产品设计任务。
- 当前 Entity 圈讨论问题入口在 `L2_proposals/[对象]__L2提案.md`；正式文档最新信息仍看 `BK_Copilot/` 对应草案；`Entity相关的所有问题.md` 已归档，不再更新。
- `未解决问题暂存清单.md` 已废弃，只作为历史参考，不再作为当前问题入口。
- `Case Settlement` 未在当前有效材料中作为正式节点出现；用户口中的 Case Settlement 经复核应指 `Case Judgment`。

已完成：

- 旧审计宪章文档已改名为 `审计目标与原则.md`。
- 已新增 `项目背景速读.md`，作为新窗口快速理解 AI Bookkeeping 项目背景的短文档。
- 根部治理文档已中文化。
- Old system 材料已被明确定位为审计对象，不是历史意图权威、评判标准或 baseline。
- New system 材料已被明确定位为审计对象，不是正确性证明、当前 authority 或 implementation-ready contract。
- 已删除治理文档中主要会偷渡产品设计判断、成功标准或功能逻辑解释的内容。
- 已读取 `new system/new_system.md`。
- 已盘点 `new system/node_stage_designs/`：共有 15 个 node。
- 已将每个 node 的 Stage 1 / Stage 2 / Stage 3 合并为一份对应的 `*_node__design.md`。
- 合并文档保留定位、逻辑边界、contract 字段摘要和 Open Boundaries；删除阶段说明、examples、self-review、历史读档记录和重复解释。
- 已新增 `系统上下文地图.md`，提炼材料结构和来源边界，供新窗口快速建立上下文；New system / Old system 只作为审计对象。
- 已将最新标准 spec 模板规则放入 `BK_Copilot/node:layer spec template/`，并拆分为 workflow node 与 memory layer 两份模板规则文件。
- 已按 workflow node 标准模板新增 `BK_Copilot/workflow_nodes/entity_resolution_node/`，目前只覆盖 Stage 1-2，不创建 Stage 3 data contract。
- 已新增 `Entity相关的所有问题.md`，现已归档为历史 Entity 讨论记录，不再作为后续问题入口。
- `TEMP_ENTITY_CASE_LOG_HANDOFF.md` 的内容已吸收进 `Entity相关的所有问题.md`。
- 已解决早期历史 Entity 问题中的问题 2、问题 3 和问题 4。
- 已确认问题 5 不属于 Entity 本身，标注为后续处理 Coordinator 时处理。
- Entity / Alias 相关已确认结论已记录在历史主题记录、`Alias question.md` 和 `BK_Copilot/` 对应正式草案中；后续 Entity 讨论问题写入 `L2_proposals/`，正式文档最新信息仍看 `BK_Copilot/`。
- 已将 BK_Copilot 正式 Entity Log / Entity Resolution 文档对齐到最新 Entity 讨论。
- 已新增 `BK_Copilot/memory_layers/alias_log/`，作为 Alias Log 的正式 memory layer 草案入口，目前覆盖 M1 Memory Intent 和 M2 Authority / Lifecycle / Boundaries；字段、技术形态和写入路径未冻结。
- 已新增并回填 `BK_Copilot/memory_layers/case_log/`，作为 Case Log 的正式 memory layer 草案入口，目前覆盖 M1 Memory Intent 和 M2 Authority / Lifecycle / Boundaries；核心来源包括 Case Log L2 提案、`BK_Copilot/` 既有正式草案 / 边界笔记和已归档 Entity 讨论记录。
- 已新增 `Rule Match临时文档.md`，记录当前 Rule Match / Rule Log 讨论中关于 rule scope、entity-level rule、pattern-level rule 和 Alias 边界的临时结论。
- Alias 写入问题已迁移到 Alias / AliasLog 主题讨论；automation policy 不作为 AliasLog L2 的当前收口项。
- 已将 AI 联网搜索、unknown identity runtime context、Rule Match 已确认边界和 `new_stable_entity` 最小创建 provenance 同步进 `BK_Copilot/` 下 Entity Resolution / Entity Log 正式草案。
- stable entity 判断 / 自动 stable 边界已收口到 `L2_proposals/Entity Resolution__L2提案.md`。
- 已将 Entity Resolution 的 stable / unknown 阈值、stable 输出 reason、exact Alias match 的 ER 语义收口成 L2 提案：`L2_proposals/Entity Resolution__L2提案.md`。
- 已将 Alias Log 的存在理由、写入资格、exact match authority、多匹配交给 ER、merge / split 语义和唯一 reader 收口成 L2 提案：`L2_proposals/Alias Log__L2提案.md`。
- 已将 Entity Log 的 identity authority 主档案定位、Entity/Alias 边界、五类保存集合、automation_policy 归属、stable entity 创建入口、lifecycle、merge / split Entity 侧边界和 person 类型边界收口成 L2 提案：`L2_proposals/Entity Log__L2提案.md`。
- 已将 Case Log 的定位、写入资格、与 Transaction Log 边界、真值单位、`use_level` 复用权限、最小 supersession 复用安全、candidate 边界、merge / split 服从治理和保存信息清单收口成 L2 提案：`L2_proposals/Case Log__L2提案.md`。
- 已按 Case Log L2 提案回填 `BK_Copilot/memory_layers/case_log/` 正式 M1-M2 草案。
- 已将 RuleMatchNode 的核心定位、trigger / routing、EntityLog / RuleLog 分工、rule scope、non-LLM 执行语义、rule hit / miss / invalid handoff、JE Generator 边界、runtime-only / finalization 写入边界、rule-hit handoff 和 governance 排除收进 L2 提案：`L2_proposals/Rule Match__L2提案.md`。
- 已补完 RuleMatchNode 收尾 L2 并实质完成节点侧 L2：新增"Rule 资格的内容判据"（全函数：scope 可客观判定 + 历史 treatment 唯一无分叉 + 输出 judgment-free 完整 + 事前已批准；次数 / 跨月只是证据非门槛；repeated cases / 相似描述 / LLM reasoning / CaseLog 趋势 / 模糊语言只能进 candidate）、"同一 entity 下 rule 集合结构"（多条 active rule 允许且互不重叠为写入时不变量；entity-level 与区分性 pattern-level 同 scope 互斥；"默认+例外"建模为互斥 pattern + catch-all；0/1/≥2 命中 = miss/hit/fail-closed blocked，不设 priority）、"RuleMatch 输入 handoff 契约面"（三桶：身份 entity_id / 放行 automation_policy 投影 / 客观交易事实；最终分类 = 命中 rule 的 approved treatment + 桶 C 交易事实，桶 C 必要不充分）、"RuleLog 查询定位"（entity_id 单坐标，RuleLog 按 entity_id 索引，EntityLog 不存 rule_id 列表）、Legacy translation，并补"运行时不重算资格"。
- 已复核 ER、Alias Log、Entity Log 与 Case Log 相关权威文档：没有新的 L1-L2 未收口问题需要在进入 Rule Match 前讨论；正式 `BK_Copilot/` 草案与已完成 L2 proposal 若存在偏差，应以后者作为本轮已锁结论。
- 已删除 `Candidate Entity question.md`，并清除 Candidate Entity / Candidate Identity 相关概念；entity 状态模型从 `{stable, candidate, unknown}` 收成 `{stable, unknown}`。
- 已根据用户确认关闭历史 Role 字段问题；Role 字段目前已删除，不再作为 Entity Log 正式字段候选处理。
- 单独决策文档已删除；后续不再维护单独决策文件，已确认结论写入用户指定的长期结论文档、对应主题记录、正式 `BK_Copilot/` 文档或必要的交接记录。

## 当前目标

让治理文档保持克制：

- 新窗口能快速接续。
- 上下文满时知道如何交接。
- 规则文档不替 agent 解释当前系统功能逻辑。
- 规则文档不预设节点、字段、流程、抽象或拆分是否合理。

## 材料角色

- `AGENTS.md`：操作入口和交接纪律。
- `审计目标与原则.md`：稳定审计目的与立场约束。
- `项目背景速读.md`：项目背景速读。
- `系统上下文地图.md`：材料结构、来源边界和审计对象位置的高密度上下文速读。
- `审计阶段路线图.md`：轻量阶段导航。
- `当前任务状态.md`：当前状态和下一步。
- `new system/`：New system 材料，只作为审计对象，不具有权威参考意义。
- `BK_Copilot/node:layer spec template/workflow_node_spec_template_rules.md`：后续最新标准 workflow node spec 的模板规则。
- `BK_Copilot/node:layer spec template/memory_layer_spec_template_rules.md`：后续最新标准 memory layer spec 的模板规则。
- `BK_Copilot/workflow_nodes/entity_resolution_node/`：Entity Resolution Node 的当前标准草案，覆盖 Functional Intent 与 Logic and Boundaries。
- `BK_Copilot/memory_layers/alias_log/`：Alias Log 的当前标准草案，覆盖 M1-M2；核心来源为 `Alias question.md`。
- `BK_Copilot/memory_layers/case_log/`：Case Log 的当前标准草案，覆盖 M1-M2；已回填 Case Log L2 提案，不冻结 data contract、读取聚合、写入者或多 log finalization 机制。
- `L2_proposals/`：Entity 圈当前讨论问题和 L2 提案入口；不替代 `BK_Copilot/` 正式草案。
- `L2_proposals/Entity Resolution__L2提案.md`：ER 已完成的上游 L2 提案；后续对象应把它作为已锁结论读取。
- `L2_proposals/Alias Log__L2提案.md`：Alias Log 已完成的上游 L2 提案；后续对象应把它作为已锁结论读取。
- `L2_proposals/Entity Log__L2提案.md`：Entity Log 已完成的上游 L2 提案；后续对象应把它作为已锁结论读取。
- `L2_proposals/Case Log__L2提案.md`：Case Log 已完成的上游 L2 提案；Rule Match 应把它作为已锁结论读取。
- `L2_proposals/Rule Match__L2提案.md`：RuleMatchNode 当前部分 L2 提案；已收口一批核心结论，但仍需继续系统复核 L1-L2 缺口。
- `Entity相关的所有问题.md`：历史 Entity 讨论归档，只读参考，不再更新。
- `Alias question.md`：保留为 Alias 主题速记；其内容已同步进历史 Entity 讨论记录和正式 Entity Log / Entity Resolution 草案，不作为更高 authority。
- `Rule Match临时文档.md`：Rule Match / Rule Log 当前临时结论，记录 rule scope 和 Alias 在 Rule Match 中的位置。
- `Coordinator Question.md`：Coordinator 提问环节当前临时结论；记录 accountant 只回分类、未说清主体时的追问与情况 X / 情况 Y 区分。
- 用户后续创建的长期结论文档：长期性结论的承载位置；规则文档不承载这些结论正文。
- `old_system_nodedesign/`：Old system 材料，只作为审计对象，不具有权威参考意义。

## 下一步

下一轮审计应：

1. 从 **Rule Log** 开始 memory layer M1-M2 / L1-L2 讨论；不要回头重做 ER / Alias Log / Entity Log / Case Log / RuleMatch，除非用户明确要求。
2. 优先读取 `L2_proposals/Rule Match__L2提案.md`（RuleMatch 已实质完成，并已对 RuleLog 提出前置约束：资格判据归属、entity_id 索引、automation_policy 取值语义、rule lifecycle）。
3. 先读取 `L2_proposals/Entity Resolution__L2提案.md`、`L2_proposals/Alias Log__L2提案.md`、`L2_proposals/Entity Log__L2提案.md` 和 `L2_proposals/Case Log__L2提案.md` 作为上游已锁结论；正式草案与 proposal 若存在偏差，以 proposal 作为本轮已锁结论。
4. Rule Match 临时结论入口是 `Rule Match临时文档.md`，但它不是上游 authority；当前 Rule Match 部分结论以 `L2_proposals/Rule Match__L2提案.md` 为准。
5. 下一个窗口不要只延续本窗口提出的残留问题；应按 workflow node Stage 1-2 / L1-L2 清单系统复核 RuleMatchNode 的存在理由、触发、读取、输出、写入、权限、异常、trace、下游影响和外部依赖，判断还有哪些 L2 面未收口。
6. Candidate Entity / Candidate Identity 整条线已删除；entity 状态模型已收成 `{stable, unknown}`。后续不要再引入 candidate entity、evidence-pattern 记忆层或类别索引层等替代机制。

## 当前风险

- 治理文档可能再次膨胀成产品设计指南。
- `审计阶段路线图.md` 容易滑向定义审计成功标准。
- `审计目标与原则.md` 容易滑向解释系统应该如何设计。
- 合并文档是瘦身摘要，不再逐字保留所有原 Stage 文档解释性文本；如需追溯旧全文，应通过 git 历史查看被删除的 Stage 文件。
- Entity 讨论仍有未决问题，不能把临时记录当成正式 spec 或冻结 data contract。

## 禁止事项

- 不要在规则文档中加入产品功能逻辑细节。
- 不要在规则文档中定义节点、字段、流程或抽象的好坏标准。
- 不要把 Old system 当评判标准、历史意图权威或 baseline。
- 不要把 New system 当正确答案、当前 authority 或 implementation-ready contract。
- 不要把 `new system/` 或 `old_system_nodedesign/` 中的任何信息当作权威参考；它们都是审计对象。
- 不要在未获得用户明确要求时编辑 `new system/` 或 `old_system_nodedesign/`。
- 不要重新生成 45 份 Stage 文档，除非用户明确要求。
- 不要把未确认的 Entity 判断写入正式 `BK_Copilot/` 文档。
- 不要继续更新 `Entity相关的所有问题.md`；后续 Entity 讨论问题写入 `L2_proposals/[对象]__L2提案.md`，正式文档最新信息仍看 `BK_Copilot/`。
- 不要继续更新或使用 `未解决问题暂存清单.md` 作为当前待办入口；该文件已废弃，只能作为历史参考。
- 不要把历史 Entity 讨论记录中的内容重复长篇搬进治理文档。

## 最近交接
- Entity 讨论问题从 `L2_proposals/` 读取；正式文档最新信息看 `BK_Copilot/` 对应草案；已完成的 L2 proposal 是上游已锁结论。Case Log 已回填正式 M1-M2 草案。
- Entity Resolution L2 已完成：见 `L2_proposals/Entity Resolution__L2提案.md`。其中写入执行者 / 调用方式属于 L4 / seam-park，不阻塞 ER L2 完成。
- Alias Log L2 已完成：见 `L2_proposals/Alias Log__L2提案.md`。已锁结论包括写入资格、exact match authority、多匹配交给 ER、merge / split 语义和唯一 reader。
- Entity Log L2 已完成：见 `L2_proposals/Entity Log__L2提案.md`。已锁结论包括 identity authority 主档案、Entity/Alias 边界、五类保存集合、automation_policy 归属、stable entity 创建入口、active / merged / archived lifecycle、merge / split Entity 侧边界和 person 类型 stable entity 边界。
- Case Log L2 已完成并回填正式 M1-M2 草案：见 `L2_proposals/Case Log__L2提案.md` 与 `BK_Copilot/memory_layers/case_log/`。
- 下一步开 **Rule Log L1-L2**；RuleMatch 节点侧 L2 已实质完成，优先读取 `L2_proposals/Rule Match__L2提案.md`。
- Alias Log 已有独立正式草案入口：`BK_Copilot/memory_layers/alias_log/`。正式草案尚未回填最新 L2 提案，不包含 data contract、storage 或 Rule Match 支持。
- Case Log 已有独立正式草案入口：`BK_Copilot/memory_layers/case_log/`。当前冻结 M1-M2；不包含 data contract、writer contract、读取聚合或多 log finalization 机制。
- Rule Match 当前部分 L2 提案入口：`L2_proposals/Rule Match__L2提案.md`。已确认 RuleMatchNode 保留为独立 deterministic rule application node；不再是所有交易必经节点；只在 ER StableEntity + EntityLog automation_policy 支持 RuleMatch + RuleLog active approved rule 的语义下触发；RuleMatchNode 由 ER / handoff 获得所需 eligibility / control context，不直接读取 EntityLog；Rule authority 来自 RuleLog；Rule payload 不放 EntityLog；RuleMatchNode 不读 AliasLog / CaseLog / TransactionLog 来执行 rule；RuleMatchNode 不做 LLM / 会计判断，只输出 approved accounting treatment 给 JE Generator；保持 runtime-only，不直接写 durable logs；命中后输出最小 rule-hit handoff（至少 StableEntity ref、Rule ref、approved accounting treatment、rule_match source 语义）。
- RuleMatch 节点侧 L2 已实质完成，补充已锁结论：(1) Rule 资格内容判据 = 全函数（scope 可客观判定 + 历史 treatment 唯一无分叉 + 输出 judgment-free 完整 + 事前已批准）；次数 / 跨月 / 频率只是证据非门槛；repeated cases / 相似描述 / LLM reasoning / CaseLog 趋势 / 模糊语言只能进 candidate。(2) 同一 entity 多条 active rule 允许且互不重叠为写入时不变量；entity-level 与区分性 pattern-level 同 scope 互斥；"默认+例外"建模为互斥 pattern + catch-all；0/1/≥2 命中 = miss / hit / fail-closed blocked，不设 priority。(3) 输入 handoff 三桶契约面（身份 entity_id / 放行 automation_policy 投影 / 客观交易事实）；最终分类 = 命中 rule 的 approved treatment + 桶 C 交易事实，桶 C 必要不充分（"答案"在 RuleLog，不在桶 C）。(4) RuleLog 查询定位 = entity_id 单坐标，RuleLog 按 entity_id 索引，EntityLog 不存 rule_id 成员列表。(5) 资格内容判据、entity_id 索引、automation_policy 取值语义、rule lifecycle 是对 RuleLog 设计的前置约束。
- 待对齐：BK_Copilot `entity_log` 草案现写"RuleMatch 自行读取 EntityLog automation policy"，与本提案"经 handoff 投影、不直接读 EntityLog"不一致；以 L2 提案为准，正式草案后续对齐，本轮未改 BK_Copilot 草案。
- `Rule Match临时文档.md` 仍可作为早期讨论线索，但不高于 `L2_proposals/Rule Match__L2提案.md`。
- `Case Judgment` 是当前材料中已存在的节点名：正式 ER 草案、Case Log 草案和历史 Entity 讨论均使用该名。`Case Settlement` 未发现为当前有效节点名，用户口头提到时应按 Case Judgment 理解，除非后续明确另设节点。
- 早期历史 Entity 问题中的问题 2、问题 3、问题 4 已解决；问题 5 延后到 Coordinator 审计时处理。
- Candidate Entity / Candidate Identity 整条线已删除（含旧 Candidate Entity 问题、Case Log candidate-linked 层等）；entity 状态模型收成 `{stable, unknown}`，相关未决问题已因前提消失作废。
- stable entity 判断 / 自动 stable 边界已收口到 Entity Resolution L2 提案。
- Role 字段问题已关闭；Role 目前已删除，不再作为 Entity Log 正式字段候选处理。
- Coordinator 完整问题模板、同 batch unknown entity 去重和多 log finalization 机制不是 Entity Log 前置讨论；按对应对象后续处理。
- 已确认：entity 未解析即被 accountant 完成分类的交易只进 Transaction Log，不写 Case Log / Entity Log，不阻塞，不复活 candidate entity；身份缺口标记落点初步倾向 Transaction Log（未冻结）。Coordinator 在提问时区分情况 X（不需要主体）/ 情况 Y（主体不明），结论记录在 `Coordinator Question.md`。
