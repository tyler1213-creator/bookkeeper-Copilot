# 当前任务状态

## 文件职责

`当前任务状态.md` 是新窗口最快理解当前情况的入口。

一句话职责：告诉新窗口“现在做到哪、下一步做什么、当前别踩什么坑”。

它只记录：

- 当前在做什么
- 已经完成什么
- 下一步最具体该做什么
- 当前风险和禁止事项
- 上一个窗口留下的必要交接

它不负责长期规则、阶段理论、产品功能解释或审计结论沉淀。

长期性结论不写在本文；本文只保留必要指针，指向对应主题记录或正式 `BK_Copilot/` 文档。

## 当前状态

当前阶段：

- Phase 1，材料盘点与系统地图

当前工作：

- 正在围绕 Entity 圈按 `ER → Alias Log → Entity Log → Case Log → Rule Match → Rule Log` 顺序推进 L2 提案。
- ER L2 提案已落地到 `BK_Copilot/workflow_nodes/entity_resolution_node/` 正式草案；Alias Log L2 提案已落地到 `BK_Copilot/memory_layers/alias_log/` 正式草案；Entity Log L2 提案已落地到 `BK_Copilot/memory_layers/entity_log/` 正式草案；Case Log 已完成并回填正式 M1-M2 草案；Rule Match Node L2 已落地到 `BK_Copilot/workflow_nodes/rule_match_node/` 正式草案（建模式，新建 00/01/02，覆盖 Stage 1-2）；下一步开 **Rule Log M1-M2**——资格判据归属、entity_id 索引、automation_policy 取值语义、rule lifecycle 都在 RuleLog 落地。
- Entity 讨论是整个系统 audit 当前聚焦点，不是脱离系统审计的独立产品设计任务。
- 当前 Entity 圈讨论问题入口在 `L2_proposals/[对象]__L2提案.md`；正式文档最新信息仍看 `BK_Copilot/` 对应草案；`Entity相关的所有问题.md` 已归档，不再更新。
- `未解决问题暂存清单.md` 已废弃，只作为历史参考，不再作为当前问题入口。
- `Case Settlement` 未在当前有效材料中作为正式节点出现；用户口中的 Case Settlement 经复核应指 `Case Judgment`。

已完成：

- 旧审计宪章文档已改名为 `审计目标与原则.md`。
- 已新增 `项目背景速读.md`，作为新窗口快速理解 AI Bookkeeping 项目背景的短文档。
- 根部治理文档已中文化。
- Old system 材料已被明确定位为审计对象，不是历史意图权威、评判标准或 baseline。
- New system 材料已被明确定位为审计对象，不是正确性证明、当前 authority 或 implementation-ready contract。
- 已删除治理文档中主要会偷渡产品设计判断、成功标准或功能逻辑解释的内容。
- 已读取 `new system/new_system.md`。
- 已盘点 `new system/node_stage_designs/`：共有 15 个 node。
- 已将每个 node 的 Stage 1 / Stage 2 / Stage 3 合并为一份对应的 `*_node__design.md`。
- 合并文档保留定位、逻辑边界、contract 字段摘要和 Open Boundaries；删除阶段说明、examples、self-review、历史读档记录和重复解释。
- 已新增 `系统上下文地图.md`，提炼材料结构和来源边界，供新窗口快速建立上下文；New system / Old system 只作为审计对象。
- 已将最新标准 spec 模板规则放入 `BK_Copilot/node:layer spec template/`，并拆分为 workflow node 与 memory layer 两份模板规则文件。
- 已按 workflow node 标准模板新增 `BK_Copilot/workflow_nodes/entity_resolution_node/`，目前只覆盖 Stage 1-2，不创建 Stage 3 data contract。
- 已新增 `Entity相关的所有问题.md`，现已归档为历史 Entity 讨论记录，不再作为后续问题入口。
- `TEMP_ENTITY_CASE_LOG_HANDOFF.md` 的内容已吸收进 `Entity相关的所有问题.md`。
- 已解决早期历史 Entity 问题中的问题 2、问题 3 和问题 4。
- 已确认问题 5 不属于 Entity 本身，标注为后续处理 Coordinator 时处理。
- Entity / Alias 相关已确认结论已记录在历史主题记录、`Alias question.md` 和 `BK_Copilot/` 对应正式草案中；后续 Entity 讨论问题写入 `L2_proposals/`，正式文档最新信息仍看 `BK_Copilot/`。
- 已将 BK_Copilot 正式 Entity Log / Entity Resolution 文档对齐到最新 Entity 讨论。
- 已按 Alias Log L2 提案回填 `BK_Copilot/memory_layers/alias_log/` 正式 M1-M2 草案；写入资格、exact match authority、唯一 reader、多匹配边界、merge / split 语义和“够格自动落库、无需额外审批”已落入正式草案；字段、技术形态、非 exact matching 和写入执行机制仍未冻结。
- 已新增并回填 `BK_Copilot/memory_layers/case_log/`，作为 Case Log 的正式 memory layer 草案入口，目前覆盖 M1 Memory Intent 和 M2 Authority / Lifecycle / Boundaries；核心来源包括 Case Log L2 提案、`BK_Copilot/` 既有正式草案 / 边界笔记和已归档 Entity 讨论记录。
- 已新增 `Rule Match临时文档.md`，记录当前 Rule Match / Rule Log 讨论中关于 rule scope、entity-level rule、pattern-level rule 和 Alias 边界的临时结论。
- Alias 写入问题已迁移到 Alias / AliasLog 主题讨论；automation policy 不作为 AliasLog L2 的当前收口项。
- 已将 AI 联网搜索、unknown identity runtime context、Rule Match 已确认边界和 `new_stable_entity` 最小创建 provenance 同步进 `BK_Copilot/` 下 Entity Resolution / Entity Log 正式草案。
- stable entity 判断 / 自动 stable 边界已收口到 `L2_proposals/Entity Resolution__L2提案.md`。
- 已将 Entity Resolution 的 stable / unknown 阈值、stable 输出 reason、exact Alias match 的 ER 语义收口成 L2 提案：`L2_proposals/Entity Resolution__L2提案.md`。
- 已按 `L2_proposals/Entity Resolution__L2提案.md` 将 ER 的两态输出、Consumer 列、stable reason、external evidence、exact Alias match、new stable entity 及时 publication 和 L3 / L4 seam 边界回填到 `BK_Copilot/workflow_nodes/entity_resolution_node/00_index.md`、`01_functional_intent.md`、`02_logic_and_boundaries.md`。
- 已将 Alias Log 的存在理由、写入资格、exact match authority、多匹配交给 ER、merge / split 语义、唯一 reader 和无需额外审批的自动落库语义收口成 L2 提案，并回填正式草案：`L2_proposals/Alias Log__L2提案.md`、`BK_Copilot/memory_layers/alias_log/`。
- 已将 Entity Log 的 identity authority 主档案定位、Entity/Alias 边界、五类保存集合、automation_policy 归属、stable entity 创建入口、lifecycle、merge / split Entity 侧边界和 person 类型边界收口成 L2 提案：`L2_proposals/Entity Log__L2提案.md`。
- 已按 Entity Log L2 提案与用户澄清回填 `BK_Copilot/memory_layers/entity_log/` 正式 M1-M2 草案；同步落地 Rule Match 只经 ER handoff projection 获得 automation_policy / eligibility、Alias entity-centered 归属、risk_flags 收窄、无自由文本 note 字段、创建入口无需 governance approval 且及时可见。
- 已将 Case Log 的定位、写入资格、与 Transaction Log 边界、真值单位、`use_level` 复用权限、最小 supersession 复用安全、candidate 边界、merge / split 服从治理和保存信息清单收口成 L2 提案：`L2_proposals/Case Log__L2提案.md`。
- 已按 Case Log L2 提案回填 `BK_Copilot/memory_layers/case_log/` 正式 M1-M2 草案。
- 已按用户审定澄清二次落地 Case Log M1-M2 正式草案：`alias` 仅为本笔交易原文快照且 Case Log 零读取 Alias Log；`confirmed_by` 命名与 system 高置信复用语义已定。
- 已按用户审定结论将 entity / 类别级通则批注 / 快捷经验的 Case Log 边界回填到 `BK_Copilot/memory_layers/case_log/`、`L2_proposals/Case Log__L2提案.md`、`L2_proposals/Rule Log__L2提案.md` 与 `缺口地图.md`：不进 Case Log 存储型基础背景层，归 entity_level Knowledge Summary；读取方向已定，具体 co-read source memory 留 KS / Case Judgment seam。
- 已完成 Rule Match Node L2 提案：`L2_proposals/Rule Match__L2提案.md`。
- 已按用户审定的 Rule Match Node L2 提案与澄清落地 `BK_Copilot/workflow_nodes/rule_match_node/` 正式草案（建模式，新建 `00_index.md`、`01_functional_intent.md`、`02_logic_and_boundaries.md`；覆盖 Stage 1-2，未创建 Stage 3）。
- 已复核 ER、Alias Log、Entity Log 与 Case Log 相关权威文档：没有新的 L1-L2 未收口问题需要在进入 Rule Match 前讨论；ER、Alias Log、Entity Log、Case Log 正式草案已按 L2 提案回填。
- 已删除 `Candidate Entity question.md`，并清除 Candidate Entity / Candidate Identity 相关概念；entity 状态模型从 `{stable, candidate, unknown}` 收成 `{stable, unknown}`。
- 已根据用户确认关闭历史 Role 字段问题；Role 字段目前已删除，不再作为 Entity Log 正式字段候选处理。
- 单独决策文档已删除；后续不再维护单独决策文件，已确认结论写入用户指定的长期结论文档、对应主题记录、正式 `BK_Copilot/` 文档或必要的交接记录。

## 当前目标

让治理文档保持克制：

- 新窗口能快速接续。
- 上下文满时知道如何交接。
- 规则文档不替 agent 解释当前系统功能逻辑。
- 规则文档不预设节点、字段、流程、抽象或拆分是否合理。

## 材料角色

- `AGENTS.md`：操作入口和交接纪律。
- `审计目标与原则.md`：稳定审计目的与立场约束。
- `项目背景速读.md`：项目背景速读。
- `系统上下文地图.md`：材料结构、来源边界和审计对象位置的高密度上下文速读。
- `缺口地图.md`：Entity 圈当前 L2 / L3 / L4 / seam 缺口索引；记录未冻结边界和圈外依赖，不替代正式草案或 L2 提案。
- `审计阶段路线图.md`：轻量阶段导航。
- `当前任务状态.md`：当前状态和下一步。
- `new system/`：New system 材料，只作为审计对象，不具有权威参考意义。
- `BK_Copilot/node:layer spec template/workflow_node_spec_template_rules.md`：后续最新标准 workflow node spec 的模板规则。
- `BK_Copilot/node:layer spec template/memory_layer_spec_template_rules.md`：后续最新标准 memory layer spec 的模板规则。
- `BK_Copilot/workflow_nodes/entity_resolution_node/`：Entity Resolution Node 的当前标准草案，覆盖 Functional Intent 与 Logic and Boundaries；已回填 Entity Resolution L2 提案，保持 Stage 1-2，不冻结 Stage 3 data contract。
- `BK_Copilot/workflow_nodes/rule_match_node/`：Rule Match Node 的当前标准草案，覆盖 Functional Intent 与 Logic and Boundaries；已回填 Rule Match L2 提案和用户澄清，保持 Stage 1-2，不冻结 Stage 3 data contract 或 L4 execution seam。
- `BK_Copilot/memory_layers/alias_log/`：Alias Log 的当前标准草案，覆盖 M1-M2；已回填 Alias Log L2 提案，`Alias question.md` 仅作为历史主题速记。
- `BK_Copilot/memory_layers/entity_log/`：Entity Log 的当前标准草案，覆盖 M1-M2；已回填 Entity Log L2 提案与用户澄清，不冻结 data contract、存储 / projection、写入执行者或多 log finalization 机制。
- `BK_Copilot/memory_layers/case_log/`：Case Log 的当前标准草案，覆盖 M1-M2；已回填 Case Log L2 提案，不冻结 data contract、读取聚合、写入者或多 log finalization 机制；entity / 类别级通则批注 / 快捷经验已明确不由 Case Log 存储，归 entity_level Knowledge Summary。
- `L2_proposals/`：Entity 圈当前讨论问题和 L2 提案入口；不替代 `BK_Copilot/` 正式草案。
- `L2_proposals/Entity Resolution__L2提案.md`：ER 已完成的上游 L2 提案；后续对象应把它作为已锁结论读取。
- `L2_proposals/Alias Log__L2提案.md`：Alias Log 已完成且已回填正式草案的 L2 提案；后续对象应把它作为已锁结论读取。
- `L2_proposals/Entity Log__L2提案.md`：Entity Log 已完成且已回填正式草案的上游 L2 提案；后续对象应把它作为已锁结论读取。
- `L2_proposals/Case Log__L2提案.md`：Case Log 已完成的上游 L2 提案；Rule Match 应把它作为已锁结论读取。
- `L2_proposals/Rule Match__L2提案.md`：RuleMatchNode 已完成 L2 提案；已落地到 `BK_Copilot/workflow_nodes/rule_match_node/` 正式草案。
- `Entity相关的所有问题.md`：历史 Entity 讨论归档，只读参考，不再更新。
- `Alias question.md`：保留为 Alias 主题速记；其内容已同步进历史 Entity 讨论记录和正式 Entity Log / Entity Resolution 草案，不作为更高 authority。
- `Rule Match临时文档.md`：Rule Match / Rule Log 当前临时结论，记录 rule scope 和 Alias 在 Rule Match 中的位置。
- `Coordinator Question.md`：Coordinator 提问环节当前临时结论；记录 accountant 只回分类、未说清主体时的追问与情况 X / 情况 Y 区分。
- 用户后续创建的长期结论文档：长期性结论的承载位置；规则文档不承载这些结论正文。
- `old_system_nodedesign/`：Old system 材料，只作为审计对象，不具有权威参考意义。

## 下一步

下一轮审计应：

1. 从 **Rule Log** 开始 memory layer M1-M2 / L1-L2 讨论；不要回头重做 ER / Alias Log / Entity Log / Case Log / RuleMatch，除非用户明确要求。
2. 优先读取 `BK_Copilot/workflow_nodes/rule_match_node/` 与 `L2_proposals/Rule Match__L2提案.md`；RuleMatch 已对 RuleLog 提出前置约束：资格判据归属、entity_id 索引、automation_policy 取值语义、rule lifecycle。
3. 先读取 `BK_Copilot/workflow_nodes/entity_resolution_node/` 和 `L2_proposals/Entity Resolution__L2提案.md` 作为 ER 已回填标准；再读取已回填的 `BK_Copilot/memory_layers/alias_log/`、`BK_Copilot/memory_layers/entity_log/`、`BK_Copilot/memory_layers/case_log/`，以及对应 `L2_proposals/Alias Log__L2提案.md`、`L2_proposals/Entity Log__L2提案.md`、`L2_proposals/Case Log__L2提案.md` 作为上游已锁结论。
4. 读取 `缺口地图.md` 查看当前 L2 / L3 / L4 / seam 缺口；其中 Case Judgment（圈外引用·仅记接缝）只记录 entity_level KS 注入方向和 co-read 外阻，不代表 Case Judgment 正式草案已存在。
5. Rule Match 临时结论入口是 `Rule Match临时文档.md`，但它不是上游 authority；当前 Rule Match 正式结论以 `BK_Copilot/workflow_nodes/rule_match_node/` 为准。
6. 下一个窗口不要继续系统复核 RuleMatchNode；按已落地正式草案把 RuleMatch 当作 RuleLog 的只读上游约束。
7. Candidate Entity / Candidate Identity 整条线已删除；entity 状态模型已收成 `{stable, unknown}`。后续不要再引入 candidate entity、evidence-pattern 记忆层或类别索引层等替代机制。

## 当前风险

- 治理文档可能再次膨胀成产品设计指南。
- `审计阶段路线图.md` 容易滑向定义审计成功标准。
- `审计目标与原则.md` 容易滑向解释系统应该如何设计。
- 合并文档是瘦身摘要，不再逐字保留所有原 Stage 文档解释性文本；如需追溯旧全文，应通过 git 历史查看被删除的 Stage 文件。
- Entity 讨论仍有未决问题，不能把临时记录当成正式 spec 或冻结 data contract。

## 禁止事项

- 不要在规则文档中加入产品功能逻辑细节。
- 不要在规则文档中定义节点、字段、流程或抽象的好坏标准。
- 不要把 Old system 当评判标准、历史意图权威或 baseline。
- 不要把 New system 当正确答案、当前 authority 或 implementation-ready contract。
- 不要把 `new system/` 或 `old_system_nodedesign/` 中的任何信息当作权威参考；它们都是审计对象。
- 不要在未获得用户明确要求时编辑 `new system/` 或 `old_system_nodedesign/`。
- 不要重新生成 45 份 Stage 文档，除非用户明确要求。
- 不要把未确认的 Entity 判断写入正式 `BK_Copilot/` 文档。
- 不要继续更新 `Entity相关的所有问题.md`；后续 Entity 讨论问题写入 `L2_proposals/[对象]__L2提案.md`，正式文档最新信息仍看 `BK_Copilot/`。
- 不要继续更新或使用 `未解决问题暂存清单.md` 作为当前待办入口；该文件已废弃，只能作为历史参考。
- 不要把历史 Entity 讨论记录中的内容重复长篇搬进治理文档。

## 最近交接
- Entity 讨论问题从 `L2_proposals/` 读取；正式文档最新信息看 `BK_Copilot/` 对应草案；已完成的 L2 proposal 是上游已锁结论。Alias Log、Entity Log 与 Case Log 已回填正式 M1-M2 草案。
- Entity Resolution L2 已完成并已回填正式草案：见 `L2_proposals/Entity Resolution__L2提案.md` 与 `BK_Copilot/workflow_nodes/entity_resolution_node/`。其中 entity_resolution_output exact schema、new stable entity provenance schema、external evidence reference schema 属于 L3；写入执行者 / 调用方式 / 顺序属于 L4 / seam-park，不阻塞 ER L2 完成。
- Alias Log L2 已完成并回填正式 M1-M2 草案：见 `L2_proposals/Alias Log__L2提案.md` 与 `BK_Copilot/memory_layers/alias_log/`。已锁结论包括写入资格、exact match authority、多匹配交给 ER、merge / split 语义、唯一 reader，以及够格自动落库、无需额外治理 / accountant 审批；写入执行者 / 时机仍属 L4 / seam。
- Entity Log L2 已完成并回填正式 M1-M2 草案：见 `L2_proposals/Entity Log__L2提案.md` 与 `BK_Copilot/memory_layers/entity_log/`。已锁结论包括 identity authority 主档案、Entity/Alias 边界、五类保存集合、automation_policy 归属、risk_flags 收窄、Rule Match 经 ER handoff projection 获得 automation_policy / eligibility、stable entity 创建入口、active / merged / archived lifecycle、merge / split Entity 侧边界和 person 类型 stable entity 边界。
- Case Log L2 已完成并回填正式 M1-M2 草案：见 `L2_proposals/Case Log__L2提案.md` 与 `BK_Copilot/memory_layers/case_log/`。entity / 类别级通则批注 / 快捷经验的 Case Log 边界也已回填：不进 Case Log 存储型基础背景层，归 entity_level Knowledge Summary；具体 co-read source memory 留 KS / Case Judgment seam。
- Case Log M1-M2 已按审定澄清二次落地：`alias` 仅是本笔交易 raw surface text 快照、Case Log 对 Alias Log 零读取权限；`confirmed_by` 命名已统一，system 高置信 case 只是强辅助先例证据，不自动授予未来自动化放行，自动化仍需 accountant 深度参与。
- `缺口地图.md` 已记录 Case Judgment（圈外引用·仅记接缝）：按 `entity_id` 锁定后按需加载 entity_level KS 作为 Case Judgment 上下文；KS 的 `authority_labels` / `downstream_usage_limits` 是建议不被当规则的护栏；具体同步 co-read 哪些 source memory 仍待 KS / Case Judgment L2 收口。
- 下一步开 **Rule Log M1-M2 / L1-L2**；Rule Match Node L2 已落地正式草案，优先读取 `BK_Copilot/workflow_nodes/rule_match_node/` 与 `L2_proposals/Rule Match__L2提案.md`。
- Alias Log 已有独立正式草案入口：`BK_Copilot/memory_layers/alias_log/`。正式草案已回填最新 L2 提案；不包含 data contract、storage、非 exact matching policy 或写入执行机制。Rule Match 不直接读取 Alias Log，只能通过 ER 输出的 stable entity 间接受益。
- Case Log 已有独立正式草案入口：`BK_Copilot/memory_layers/case_log/`。当前冻结 M1-M2；不包含 data contract、writer contract、读取聚合或多 log finalization 机制。
- Rule Match 正式草案入口：`BK_Copilot/workflow_nodes/rule_match_node/`；对应已审定提案见 `L2_proposals/Rule Match__L2提案.md`。RuleMatch 对 RuleLog 的前置约束和未冻结边界见 `00_index.md`。
- 已闭合：BK_Copilot `entity_log` 草案已改为 RuleMatch 不直接读取 EntityLog；Entity Resolution 读取 EntityLog 后，将 automation_policy / eligibility 当前状态投影进 handoff 传给 RuleMatch。
- `Rule Match临时文档.md` 仍可作为早期讨论线索，但不高于 `L2_proposals/Rule Match__L2提案.md`。
- `Case Judgment` 是当前材料中已存在的节点名：正式 ER 草案、Case Log 草案和历史 Entity 讨论均使用该名。`Case Settlement` 未发现为当前有效节点名，用户口头提到时应按 Case Judgment 理解，除非后续明确另设节点。
- 早期历史 Entity 问题中的问题 2、问题 3、问题 4 已解决；问题 5 延后到 Coordinator 审计时处理。
- Candidate Entity / Candidate Identity 整条线已删除（含旧 Candidate Entity 问题、Case Log candidate-linked 层等）；entity 状态模型收成 `{stable, unknown}`，相关未决问题已因前提消失作废。
- stable entity 判断 / 自动 stable 边界已收口并回填到 Entity Resolution 正式草案。
- Role 字段问题已关闭；Role 目前已删除，不再作为 Entity Log 正式字段候选处理。
- Coordinator 完整问题模板、同 batch unknown identity 去重和多 log finalization 机制不是 Entity Log 前置讨论；按对应对象后续处理。
- 已确认：entity 未解析即被 accountant 完成分类的交易只进 Transaction Log，不写 Case Log / Entity Log，不阻塞，不复活 candidate entity；身份缺口标记落点初步倾向 Transaction Log（未冻结）。Coordinator 在提问时区分情况 X（不需要主体）/ 情况 Y（主体不明），结论记录在 `Coordinator Question.md`。
