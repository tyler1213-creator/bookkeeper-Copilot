# 未解决问题暂存清单

## 文件角色

这是临时问题清单。

只有用户明确同意记录的问题，才可以写入这里。

Assistant 不得自行判断哪些问题应写入。

本文件不是正式 spec。

## 待记录

### 问题 1：Stable entity 写入时，role 和 alias 由谁负责写入？

Entity Resolution 同步写入 stable entity 本体已确认。但以下附属信息的写入时机和责任未定：

- 当前 surface text 应成为 approved alias、candidate alias、还是暂不写入？
- Role 应在何时由谁写入？
- Approved alias、confirmed role、automation policy 的后续写入路径是什么？

注意：即便 surface text 成为 approved alias，也不意味着 Rule Match 可以直接使用。每一个 rule 必须经过 accountant 审核才可启用。Alias 只是让后续交易可以匹配到同一个 entity，不等于自动获得 rule。

### 问题 2：Case Judgment 判断会计分类（COA）是否必须依赖已识别的 entity？（已解决）

**结论：** Case Judgment 的高置信度自动分类通道必须依赖已识别的 entity。Entity Resolution 输出 unknown_entity 时，Case Judgment 不走高置信度分类通道，但仍然处理这笔交易，输出 pending。

Pending 内部分两种情况：

1. **完全无法判断**：没有足够上下文支持推断，由 Coordinator 直接向 accountant 提问。
2. **有推断但不确定**：Case Judgment 利用已有的其他上下文（receipt items、交易金额模式等）给出推断性建议，由 Coordinator 呈现给 accountant 选择或确认。

三个子问题的回答：

- 是否存在不需要 entity 就能判断 COA 的交易？ → 存在（bank fee、interest 等），但属于 edge case，大部分由 Profile / Structural Match Node 在上游处理。到达 Case Judgment 的交易绝大多数需要 entity identity。
- unknown_entity 时 Case Judgment 能否用推测辅助分类？ → 不做高置信度分类，但可以给出推断性建议附在 pending 输出中供 accountant 选择。
- 基于不确定 identity 的高置信度分类后续如何修正？ → 不存在这个场景，因为不会产生高置信度分类。

A/B 分类判断（是否需要 entity 才能分类）的职责在 Case Judgment，不在 Entity Resolution。Entity Resolution 只管 identity。

详细推理链记录在 `Entity相关的所有问题.md` 的"Case Judgment 与 Entity Identity 的依赖关系"部分。

### 问题 3：Entity Resolution 输出 unknown_entity 时，是否应附带对 entity 的推测信息？

Entity Resolution 无法判断 stable entity 时，是否应输出类似 "我猜可能是 X" 的推测信息传给下游节点？

待讨论的点：

- 推测信息的内容和格式是什么？
- 下游节点（Case Judgment、Coordinator）如何使用这些推测？
- 推测是否需要标记置信度？
- 基于推测做的会计分类是否需要带 `identity_basis: uncertain` 标记？

### 问题 4：Entity Resolution 输出 unknown entity 后的处理路径应该如何设计？

当前倾向（待确认）：

- 如果 Entity Resolution 无法识别出 entity 是谁，Case Judgment 不应输出高置信度的会计分类。
- 倾向于：只要 entity 未被识别为 stable，整笔交易直接 pending，由 Coordinator 生成结构化问题让 accountant 回答。
- 这样减少系统复杂度：不需要先给一个可能错误的分类，再在 entity 确认后重新验证和修复。
- Accountant 一次性审阅的信息可能更多，但避免了系统反复修正的开销。

待讨论：这个倾向是否应用于所有 unknown entity 交易，还是存在例外（如 bank fee 等不依赖 entity 的交易类型）？

### 问题 5：Entity 未被识别时 Coordinator 的结构化问题应如何设计？

两种场景：

- 场景 A：有其他有效证据（如 receipt）可供 accountant 参考，但 entity 不清楚。Coordinator 应先问 entity 是谁，accountant 回答后系统判断分类。
- 场景 B：entity 不清楚且缺少足够证据。Coordinator 需要同时提供 entity 和分类相关的结构化问题，但问题应有结构：先确认 entity，再基于确认后的 entity 询问分类。

待讨论：Coordinator 的问题模板、结构和 accountant 回答后的处理流程。

### 问题 6：同一 batch 内重复出现的 unknown entity 如何避免重复向 accountant 提问？

当前倾向：Coordinator 对同一 batch 内相同 descriptor pattern 的 unknown entity 问题做去重合并，只向 accountant 提一次。

待确认：

- 现有 Coordinator 相关文档是否已覆盖此场景？
- 老系统 Coordinator Agent 的去重逻辑可作为参考。
- 去重的匹配粒度是什么（exact descriptor match、semantic similarity、还是其他）？

### 问题 7：交易完成分类后的多 log 统一写入机制应该如何设计？

交易完成分类后，需要写入多个 log（Entity Log、Case Log、Transaction Log、Intervention Log 等）。这些写入操作应该由谁管理？

待讨论的点：

- 是否需要一个统一的 Memory Update / Finalization 步骤来管理所有持久化写入？
- 现有的 Case Memory Update Node 是否可以扩展为这个角色？
- 还是应该让各个节点（如 Coordinator）在自己的流程中直接执行写入？
- 老系统中 Coordinator 直接调用写入脚本（write_observation.py、write_transaction_log.py 等）的模式在新系统中是否仍然适用？
- Entity Resolution 自动创建 stable entity 的写入路径与 accountant 确认后创建 stable entity 的写入路径是否应共用同一机制？
- Stable entity 的具体创建由哪个节点或机制执行？（关联 `Entity相关的所有问题.md` 第 5 条开放问题"Candidate -> stable 谁来执行"）
