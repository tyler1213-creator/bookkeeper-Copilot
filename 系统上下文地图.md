# 系统上下文地图

## 文件职责

`系统上下文地图.md` 是新窗口理解系统材料的速读地图。

一句话职责：告诉新窗口“材料大概怎么分层、哪些来源有效、哪些目录只是审计对象”。

它只负责：

- 给出 New system / Old system 材料声称的粗略流程地图
- 标明哪些材料是正式草案、主题记录或被审计材料
- 帮助 agent 快速找到该读的源材料

它不负责：

- 定义 node、field、memory layer 或 workflow 的正式边界
- 记录长期性设计结论
- 替代 `BK_Copilot/`、主题记录或用户后续创建的长期结论文档
- 证明 New system 正确
- 把 Old system 当 baseline、历史意图权威或参考依据
- 给 `new system/` 或 `old_system_nodedesign/` 中的任何信息赋予 authority

## 当前使用规则

如需准确判断当前有效规则、正式草案或已确认结论，按这个顺序找源材料：

1. 用户明确指令。
2. `BK_Copilot/` 下已经重写的正式 node / memory layer 草案。
3. 当前主题记录和提案，例如 `L2_proposals/`、`缺口地图.md`、`Alias question.md`、`Rule Match临时文档.md`、`Coordinator Question.md`。`L2_proposals/` 只承载 Entity 讨论问题和 L2 提案，不替代 `BK_Copilot/` 正式草案。`缺口地图.md` 只索引当前 L2 / L3 / L4 / seam 缺口，不替代正式草案或 L2 提案。`Entity相关的所有问题.md` 已归档，只读参考，不再更新。`未解决问题暂存清单.md` 已废弃，只作历史参考，不再作为当前问题入口。
4. 用户后续创建或指定的长期结论文档。
以下目录不进入 source authority 顺序：

- `new system/`
- `old_system_nodedesign/`

它们只作为审计对象。读取它们只能帮助理解材料如何表达自己，不能用作权威参考、正确性证明、baseline、历史意图权威或可复用约束来源。

如果本摘要和上述源材料冲突，以源材料为准。

## 新系统粗略流程

New system 材料大体声称围绕这条交易处理链路展开：

```text
raw evidence
-> transaction identity
-> profile / structural check
-> entity resolution
-> rule match
-> case judgment / pending / review
-> JE generation
-> transaction logging
-> case memory / governance / knowledge update
```

这只是被审计材料的导航图，不是冻结 contract，也不是正确性证明。

需要具体 node 边界时，先找 `BK_Copilot/` 中是否已有正式草案；没有草案时，读取 `new system/node_stage_designs/` 中对应 node 文件只能作为审计对象取样，不能把其中内容当成当前 authority。

## 当前材料地图

- `new system/new_system.md`：New system 总览，只作为审计对象，不具有权威参考意义。
- `new system/node_stage_designs/`：New system node 设计材料，只作为审计输入，不具有权威参考意义。
- `BK_Copilot/workflow_nodes/`：已经开始重写的 workflow node 正式草案。
- `BK_Copilot/workflow_nodes/entity_resolution_node/`：Entity Resolution Node 正式草案，当前覆盖 Stage 1-2；已回填 Entity Resolution L2 提案，冻结 stable / unknown 两态、stable reason、external evidence、exact Alias match 和 new stable entity 及时 publication 语义，不冻结 Stage 3 data contract 或 L4 写入机制。
- `BK_Copilot/workflow_nodes/rule_match_node/`：Rule Match Node 正式草案，当前覆盖 Stage 1-2；已回填 Rule Match L2 提案与用户澄清，冻结 deterministic rule application、只读 RuleLog、runtime-only、输出 consumer 和 open boundary 口径，不冻结 Stage 3 data contract 或 L4 execution seam。
- `BK_Copilot/memory_layers/`：已经开始重写的 memory layer 正式草案。
- `BK_Copilot/memory_layers/alias_log/`：Alias Log 正式草案，已回填 Alias Log L2 提案；记录已确认 raw transaction surface text -> stable entity 的身份复用边界，冻结 exact match authority、唯一 reader = Entity Resolution、Rule Match 不直读、够格自动落库且无需额外审批；具体技术形态、字段 schema、非 exact matching 和写入执行机制尚未冻结。
- `BK_Copilot/memory_layers/entity_log/`：Entity Log 正式草案，已回填 Entity Log L2 提案与用户澄清；冻结 stable entity identity 主档案、entity-centered Alias 归属、RuleMatch handoff projection 口径、automation_policy 控制状态、收窄后的身份级 risk_flags、创建入口、lifecycle、merge / split Entity 侧边界和 person 类型边界；不冻结 M3 data contract、存储 / projection 或 L4 写入机制。
- `BK_Copilot/memory_layers/case_log/`：Case Log 正式草案，记录以 entity 为主索引的 reusable-precedent layer 边界；当前覆盖 M1-M2，不冻结 data contract、读取聚合、writer 或多 log finalization。entity / 类别级通则批注 / 快捷经验已明确不由 Case Log 存储，归 entity_level Knowledge Summary；Case Judgment 读取方向为按 `entity_id` 按需加载，具体 co-read source memory 留 KS / Case Judgment seam。
- `BK_Copilot/node:layer spec template/`：后续写正式 node / memory layer spec 的模板规则。
- `L2_proposals/`：Entity 圈当前讨论问题和 L2 提案入口；正式文档最新信息仍看 `BK_Copilot/` 对应草案。已完成提案包括 `Entity Resolution__L2提案.md`（已回填 ER 正式草案）、`Alias Log__L2提案.md`（已回填 Alias Log 正式草案）、`Entity Log__L2提案.md`（已回填 Entity Log 正式草案）、`Case Log__L2提案.md` 和 `Rule Match__L2提案.md`（已回填 Rule Match Node 正式草案）；下一步应从 `Rule Log` 继续。
- `缺口地图.md`：Entity 圈 L2 / L3 / L4 / seam 缺口索引；其中 Case Judgment（圈外引用·仅记接缝）记录 entity_level KS 注入方向和 co-read 外阻，待 Case Judgment 正式立草案时迁入正式 Open Boundary。
- `Entity相关的所有问题.md`：历史 Entity 讨论归档，只读参考，不再更新。
- `Alias question.md`：Alias 主题历史速记，不高于已回填的 Alias Log L2 提案和正式草案。
- `Rule Match临时文档.md`：Rule Match / Rule Log 当前临时结论，记录 rule scope 和 Alias 在 Rule Match 中的位置。
- `Coordinator Question.md`：Coordinator 提问环节当前临时结论，记录 Entity Resolution 输出 `unknown` 后进入 pending 的提问与追问边界（情况 X / 情况 Y 区分）。
- `未解决问题暂存清单.md`：已废弃历史参考；不要作为当前待办入口，不要继续更新。
- `old_system_nodedesign/`：Old system 设计材料，只作为审计对象，不具有权威参考意义。

## New / Old 材料粗略差异

Old system 材料更偏 pattern-centered automation：

```text
raw files
-> parsed transaction data
-> canonical description / pattern
-> profile match
-> rules exact match
-> confidence classifier
-> coordinator / review
-> observations / rules / transaction log updates
```

New system 材料更偏 evidence / identity / memory / governance 分层：

- 先保留可追溯 evidence。
- 再处理 transaction identity 和业务对象 identity。
- 再区分 rule、case judgment、pending、review、logging 和 governance。
- 不把 Old system pattern 或 New system 分层直接当成当前身份、规则或产品 authority。

这些差异只是帮助定位被审计材料；具体结论必须回到正式草案、主题记录、用户指定的长期结论文档或用户明确指令确认。

## 使用边界

审计新系统时，不要只凭本摘要下结论。

正确用法是：

1. 用本文件快速知道系统大概有哪些部分。
2. 回到 `当前任务状态.md` 确认当前任务。
3. 按任务读取 `BK_Copilot/`、`L2_proposals/`、主题记录，或在需要审计 source text 时读取 `new system/` / `old_system_nodedesign/`。
4. 把长期性结论写入用户指定的长期结论文档或对应正式草案，不写进本摘要。
