# 01 契约面与缺口（Transaction Log，从 A 单独重建，只抄 A 原文 + 出处，标 △缺口）

> 纪律：本文件只抄 A 类原文（`BK_Copilot/` 下已落文邻居的正式 00/01/02 文档 + 模板规则 + 已锁事实）。B 类（`new system/`、`old_system_nodedesign/`）一字不出现。
> Transaction Log 本身是圈外 / 未落文对象（尚无 BK_Copilot 正式草案），故契约面只能从「已落文邻居在自己文档里如何引用 Transaction Log」侧面重建。
> △缺口 = A 锁不住 / 邻居未落文 / 需用户判断的开放问题，是本步最值钱产物。语义是否对齐（A 锁的够不够 Transaction Log 用）属主线程判断，此处只摆两端 A 原文，不下结论。
> 出处格式：`文件:章节/行`。

---

## 0. 邻居名单（推导依据）

A 桶（已落文邻居，逐一核实存在并已读相关部分）：
- workflow_nodes：`evidence_intake_node`、`entity_resolution_node`、`rule_match_node`、`case_judgment_node`、`coordinator_node`（各含 00/01/02 三份）
- memory_layers：`case_log`（含 `case_log_boundary_note.md`）、`entity_log`、`rule_log`、`alias_log`（各含 00/01/02 三份）

链路（`系统上下文地图.md`）指向 Transaction Log 处于 finalization 段：分类 / review / JE 之后，Transaction Log 承接「最终交易审计 source of truth」。

未落文邻居（绝不拿 B 填，挂 open boundary，见 §9）：JE Generation Node、Intervention Log、Governance / Governance Review、Profile / Structural Match、Knowledge Summary / Knowledge Compilation Node、Evidence Log、Review Node、Case Memory Update Node、interaction_agent、Post-Batch Lint。

---

## 接缝 1 — Case Judgment → Transaction Log

A 原文（CJ 文档逐项摘出凡提到 Transaction Log 处）：
- 「reasoning / audit trace 应进入 Transaction Log，但 exact 存储字段、后续治理节点读取 reasoning 的 contract、Transaction Log 写入机制仍未冻结，归 L3 / L4 / 外阻。」`case_judgment_node/00_index.md:38`
- 「`case_memory_update_candidate` 的 exact field schema、Case Memory Update Node 的写入机制、Case Log 与 Transaction Log 的 finalization trigger order 仍未冻结，归 L3 / L4 / seam。」`case_judgment_node/00_index.md:39`
- 「Coordinator、Knowledge Summary、Profile / Structural Match、JE Generation、Evidence Log、Transaction Log、Governance 等圈外对象的正式草案仍未全部落定；CJ 中对这些对象的引用不得当冻结合同。」`case_judgment_node/00_index.md:44`
- 「CJ 绝不做：直接写 Entity Log、Alias Log、Rule Log、Case Log、Transaction Log、Governance Log 或 Profile。」`case_judgment_node/01_functional_intent.md:63`
- 「后续 finalization / audit / learning 机制：负责 Transaction Log、Case Log 等持久化；CJ 只声明要持久化的语义，不声明写入者、顺序或存储机制。」`case_judgment_node/01_functional_intent.md:81`
- 「审计性：reasoning 进入 Transaction Log 或等价 audit trace；trace 可供 review / correction / governance / audit，但不成为 authority。」`case_judgment_node/01_functional_intent.md:106`
- 「High Confidence Classification 的最终交易结果、结构化 reasoning、AI 高置信度自动完成标记和相关 evidence / external source refs，应由后续 finalization / Transaction Log 机制按其资格保存；有效性来自 CJ runtime judgment、deterministic COA 校验和 finalization 对本次交易完成状态的认定。」`case_judgment_node/02_logic_and_boundaries.md:92`
- 「Pending 的 reason、已执行动作、可选候选建议和阻塞语义交 Coordinator 消费；是否以及如何进入长期 audit / interaction record 由 Coordinator / Transaction Log / finalization 设计决定。」`case_judgment_node/02_logic_and_boundaries.md:94`
- High Confidence Classification 输出表「下游影响」列：「finalized 后由后续机制在 Transaction Log / Case Log 留痕并标注 AI 高置信度自动完成」；「不代表」列：「不代表 JE 已生成成功、Transaction Log / Case Log 已写入……」`case_judgment_node/02_logic_and_boundaries.md:182`
- `case_memory_update_candidate` 输出表：「Case Memory Update Node 在交易 finalized 后凭 `transaction_log_ref` 或等价 finalization proof 处理写入资格。」`case_judgment_node/02_logic_and_boundaries.md:184`
- CJ 自挂未冻结清单：「13. reasoning 在 Transaction Log 的 exact 存储字段、后续治理节点读取 reasoning 的 contract。」「14. `case_memory_update_candidate` exact schema、Case Memory Update Node 写入机制、Case Log 与 Transaction Log finalization trigger order。」`case_judgment_node/02_logic_and_boundaries.md:398-399`

A 在此接缝锁死的「Transaction Log 要存什么 / 谁在什么时点写」：
- 存什么（语义层，非字段）：High Confidence Classification 的「最终交易结果、结构化 reasoning、AI 高置信度自动完成标记、相关 evidence / external source refs」；CJ 的 reasoning / audit trace。
- 谁写 / 时点：CJ **绝不**直接写 Transaction Log；由「后续 finalization / audit / learning 机制」在交易 finalized 后写；CJ 只声明要持久化的语义，不声明写入者 / 顺序 / 存储机制。
- 边界（什么归 Transaction Log）：reasoning 进入 Transaction Log「可供 review / correction / governance / audit，但不成为 authority」。

△缺口（A 没说 / 明示未冻结）：
- reasoning 在 Transaction Log 的 **exact 存储字段** + 后续治理节点读取 reasoning 的 contract → A 明示未冻结（CJ L3 / L4 / 外阻）。`case_judgment_node/02_logic_and_boundaries.md:398`、`缺口地图.md:123`
- Case Log 与 Transaction Log 的 **finalization trigger order** → A 明示未冻结（L3 / L4 / seam）。`case_judgment_node/00_index.md:39`
- Pending 语义「是否以及如何进入长期 audit / interaction record」→ A 明示「由 Coordinator / Transaction Log / finalization 设计决定」，即留待 Transaction Log 正式草案。`case_judgment_node/02_logic_and_boundaries.md:94`
- 「AI 高置信度自动完成标记」在 Transaction Log 的字段形态 → A 只给语义，未给字段。

---

## 接缝 2 — Coordinator → Transaction Log

A 原文（Coordinator 文档逐项摘出）：
- 「runtime 交互留痕与学习闭环入口：会计师确认后的交易无法清楚连接 Transaction Log、Entity Log、Case Log 和 Intervention Log 的各自职责。」`coordinator_node/01_functional_intent.md:14`
- 「Coordinator 绝不做：裸写 Entity Log、Alias Log、Case Log、Rule Log、Transaction Log、Intervention Log、Governance Log、Profile 或 policy / rule。」`coordinator_node/01_functional_intent.md:46`
- 「Transaction Log：承接最终交易结果审计；拿不到 entity 但完成分类的交易只 finalize 到 Transaction Log。」`coordinator_node/01_functional_intent.md:67`
- 「审计性：会计师答复、身份确认、最终交易结果和学习记录分别指向 Intervention Log、Entity Log、Transaction Log、Case Log，不互相替代。」`coordinator_node/01_functional_intent.md:95`
- 「Coordinator 不直接读取 Case Log、Rule Log、Governance Log、Profile、Knowledge Summary 或 Transaction Log。」`coordinator_node/02_logic_and_boundaries.md:47`
- 【情况 Y / 身份缺口落点 + Case Log 学习记录与 Transaction Log 分界，核心原文】「Case Log 学习记录语义：stable-linked（实体已是 stable，无论由 ER 命中还是会计师确认而来）、已 finalized、且会计师已确认 accounting outcome 的交易，可以产生 `confirmed_by=accountant` 的学习记录语义，并必须凭 Transaction Log 或等价 finalization proof。有效性来自 accountant confirmation + finalization proof。拿不到 entity 的交易（情况 X / Y）只 finalize 到 Transaction Log，不进 Case Log。」`coordinator_node/02_logic_and_boundaries.md:62`
- 「最终交易结果审计：会计师答复足够后形成的交易 finalization 语义应由 Transaction Log 或等价 finalization 机制承接；exact contract 属圈外。」`coordinator_node/02_logic_and_boundaries.md:64`
- 「Coordinator 绝不做：Case Log、Rule Log、Transaction Log、Intervention Log、Governance Log、Profile 或 Knowledge Summary 的裸写。」`coordinator_node/02_logic_and_boundaries.md:74`
- 【情况 Y 路由原文】「后续路由：这笔交易只走会计分类；分类完成后只 finalize 到 Transaction Log，不写 Entity Log / Alias Log / Case Log，不因身份未知而阻塞。」`coordinator_node/02_logic_and_boundaries.md:169`
- 【结果审计落点原文】「结果审计：最终交易 outcome、processing path、身份未知但分类已完成的 finalization，交 Transaction Log 或等价 finalization source。」`coordinator_node/02_logic_and_boundaries.md:206`
- 【三 Log 分界总述原文】「Intervention Log 只记录交互事实；结果审计不挂在 Intervention Log 上。Case Log 只保存具备 stable entity + finalization proof 的学习先例；Transaction Log 是最终交易审计 source of truth。」`coordinator_node/02_logic_and_boundaries.md:226`
- 交互留痕输出表「不代表」列：「不代表 Entity / Case / Rule / policy authority，不代表结果审计替代 Transaction Log。」`coordinator_node/02_logic_and_boundaries.md:134`
- 转 JE 输出表「不代表」列：「不代表 JE 已生成、Transaction Log 已写入、字段 contract 已冻结，或 Coordinator 自己做了分类判断。」`coordinator_node/02_logic_and_boundaries.md:129`
- Coordinator 自挂未冻结清单 / 圈外依赖：「2. Case Log 学习记录 exact writer、trigger order，以及与 Transaction Log / Entity Log 的统一 finalization。」`coordinator_node/02_logic_and_boundaries.md:262`；「2. Transaction Log：身份未知但分类完成交易的落点、finalization、结果审计 source of truth。」`coordinator_node/02_logic_and_boundaries.md:276`
- 圈外依赖列表：「L2·外阻：JE Generation、Transaction Log、Intervention Log、Governance / Governance Review、Profile / Structural Match、Knowledge Summary、Review Node、Case Memory Update Node、interaction_agent、Onboarding 等圈外对象正式边界。」`coordinator_node/00_index.md:27`；「JE Generation、Transaction Log、Intervention Log 三个圈外下游完成足以支撑 Coordinator 契约收口的正式落文。」`coordinator_node/00_index.md:32`

A 在此接缝锁死的「Transaction Log 要存什么 / 谁在什么时点写 / 边界」：
- 存什么（语义层）：最终交易 outcome、processing path、最终交易结果审计；情况 X / Y（拿不到 entity 但分类已完成）的 finalization；身份未知但分类已完成的 finalization。
- Transaction Log 定位：**最终交易审计 source of truth**。
- 谁写 / 时点：Coordinator **绝不裸写** Transaction Log，也**不读取** Transaction Log；finalization 语义「由 Transaction Log 或等价 finalization 机制承接」，会计师答复足够后形成。
- 边界（四 Log 分界，A 锁得最清楚）：交互事实 → Intervention Log；身份确认 → Entity Log；学习先例（stable entity + finalization proof）→ Case Log；最终交易结果审计 → Transaction Log；四者不互相替代。情况 X / Y 交易**只**进 Transaction Log，不进 Case Log / Entity Log。

△缺口（A 明示未冻结 / 留 Transaction Log 正式草案）：
- 「身份未知但分类完成交易的落点、finalization、结果审计 source of truth」exact contract → A 明示属圈外（L2·外阻）。`coordinator_node/02_logic_and_boundaries.md:64,276`、`缺口地图.md:184`
- 情况 Y「身份缺口落点」具体形态（标记落在 Transaction Log 上的「不阻塞复查线索」长什么样）→ A 称「初步倾向 Transaction Log，未冻结」（见接缝 5 Case Log 原文）。
- Case Log 学习记录与 Transaction Log / Entity Log 的**统一 finalization** + exact trigger order + writer → A 明示未冻结（L4/seam）。`coordinator_node/02_logic_and_boundaries.md:262`、`缺口地图.md:171`

---

## 接缝 3 — Evidence Intake → Transaction Log（多 log finalization 顺序 + transaction_id 锚点）

A 原文（Evidence Intake 文档逐项摘出）：
- 「`transaction_id` 的统一铸号点，下游 ER / JE / Transaction Log / Case lineage 会失去同一笔资金事件的稳定锚点。」`evidence_intake_node/01_functional_intent.md:13`
- 「（不冻结）Evidence Log / Transaction Log 的写入执行者、写入顺序、finalization 或回滚机制。」`evidence_intake_node/01_functional_intent.md:56`
- 「三类来源共享同一 Evidence Intake 输出契约面，以 `source_channel` 标注 provenance。本节点不读取 Transaction Log 作为 runtime source。」`evidence_intake_node/01_functional_intent.md:75`
- 「Transaction Log finalization（圈外对象，只挂起引用，不替它冻结 spec）。」`evidence_intake_node/01_functional_intent.md:83`
- 「本节点不读 Transaction Log、Entity / Case / Rule / Governance memory 作为运行时判断来源。」`evidence_intake_node/01_functional_intent.md:111`
- 「[不读] Transaction Log | 无 | 无 | 本节点不查历史交易、不复用既有 `transaction_id`、不做跨导入 dedupe。」`evidence_intake_node/02_logic_and_boundaries.md:36`
- 【多 log finalization 锚点原文】「`transaction_id + objective transaction basis + evidence binding` 应在 Transaction Log finalization 或等价最终化路径中持久化；本文不冻结该机制。」`evidence_intake_node/02_logic_and_boundaries.md:54`
- `transaction_id` 输出表 Consumer 含「Transaction Log finalization（圈外，只挂起引用，不替它冻结 spec）」；下游用途「finalization 用它持久化 id + evidence + outcome 绑定」；「不代表」列「不代表……Transaction Log 已写入」。`evidence_intake_node/02_logic_and_boundaries.md:125`
- 「不查历史、不读 Transaction Log、不访问 durable registry、不复用既有 id。」`evidence_intake_node/02_logic_and_boundaries.md:148`

A 在此接缝锁死的：
- 存什么（语义层）：`transaction_id + objective transaction basis + evidence binding` 应在 Transaction Log finalization 持久化（持久化 id + evidence + outcome 绑定）。
- 边界：Evidence Intake **不读** Transaction Log 作 runtime source；Transaction Log 是下游 finalization 对象，Evidence Intake 只挂起引用。
- 步骤约束：`transaction_id` 统一铸号点在 Evidence Intake，Transaction Log 是其下游锚点 consumer 之一。

△缺口（A 明示未冻结）：
- 多 log finalization **顺序**（Evidence / Transaction / Case / Rule / JE）+ Transaction Log 的写入执行者 / 写入顺序 / finalization / 回滚机制 → A 明示不冻结（L4/seam，且 Evidence Log 尚未正式审计）。`evidence_intake_node/01_functional_intent.md:56`、`evidence_intake_node/02_logic_and_boundaries.md:54`、`缺口地图.md:77`

---

## 接缝 4 — Rule Match / Rule Log → Transaction Log（rule-hit ref 字段形态）

A 原文（Rule Log 文档逐项摘出；Rule Match 节点 00/01/02 未出现 "Transaction Log" 字样，rule-hit ref 接缝由 Rule Log 承载）：
- 「（rule source / approval provenance refs）是 rule 生效资格 trace，不是交易审计记录，不替代 Governance Log / Transaction Log / Case Log 正文，也不单独成为会计处理 authority。」`rule_log/01_memory_intent.md:41`
- 「（不保存）Transaction Log 审计轨迹或完整交易历史。」`rule_log/01_memory_intent.md:54`
- 「Transaction Log：最终交易审计 source of truth。」`rule_log/01_memory_intent.md:66`
- 【rule-hit 统计派生原文】「rule-hit 来源语义和 rule ref 应随 finalized transaction / case context 保留；统计由 Transaction Log / Case Log 的逐笔记录派生，或由维护视图缓存。缓存不改变 Rule Log authority。」`rule_log/01_memory_intent.md:69`
- 【边界表 · Transaction Log 行原文】「Transaction Log | Transaction Log 是最终交易审计 source of truth，适合保存某笔 finalized transaction 最终由哪条 rule 处理的 source 语义和 rule ref。Rule Log 不做历史解释。 | （已确认缺口栏）rule-hit source / rule ref exact 字段形态；finalization / record path 的写入顺序；历史修正与 rule ref 追溯机制。」`rule_log/02_authority_lifecycle_and_boundaries.md:103`
- 边界表 · Case Log 行：「Rule-hit case 的 rule ref 可以由 Transaction Log / Case Log 逐笔记录派生统计。」`rule_log/02_authority_lifecycle_and_boundaries.md:102`
- 「（不替代）Transaction Log / Case Log / Governance Log 正文的替代品。」`rule_log/02_authority_lifecycle_and_boundaries.md:150`
- 多 log finalization 自挂缺口：「多 log finalization、Transaction Log / Case Log 的 rule-hit ref 写入顺序和记录机制。」`rule_log/02_authority_lifecycle_and_boundaries.md:91`
- 「L3，联合 Transaction Log / Case Log：rule-hit source / rule ref 在 Transaction Log / Case Log 中的 exact 字段形态。」`rule_log/00_index.md:37`、`rule_log/02_authority_lifecycle_and_boundaries.md:167`

A 在此接缝锁死的：
- 存什么（语义层）：某笔 finalized transaction「最终由哪条 rule 处理」的 source 语义和 rule ref，适合保存在 Transaction Log（逐笔记录）。
- 边界：Rule Log **不**保存 Transaction Log 审计轨迹 / 完整交易历史 / 历史解释；Transaction Log 才是历史解释与逐笔记录之处。统计由 Transaction Log / Case Log 逐笔记录派生。

△缺口（A 明示未冻结）：
- rule-hit source / rule ref 在 Transaction Log 的 **exact 字段形态** → A 明示（L3，联合 Transaction Log / Case Log）。`rule_log/00_index.md:37`、`缺口地图.md:105`
- finalization / record path 的写入顺序、历史修正与 rule ref 追溯机制 → A 明示未冻结。`rule_log/02_authority_lifecycle_and_boundaries.md:103`
- 统计派生 / rollup / cache / maintenance job → A 明示未冻结（L3/L4/seam）。`缺口地图.md:105`

---

## 接缝 5 — Case Log ↔ Transaction Log（存储边界划分，A 锁得最详）

A 原文（Case Log 文档逐项摘出，含 boundary note）：
- 「`Transaction Log` 是 audit-facing final transaction record；它可以通过索引支持审计 / 报表查询，但不作为主 workflow 的 entity-indexed learning layer。」`case_log/01_memory_intent.md:20`
- 字段表：「3 | `transaction_log_ref` | finalization proof / 审计锚点。证明案例来自已完成交易；也作为通往修改历史、完整 JE、原文等审计留痕的桥。」`case_log/01_memory_intent.md:34`
- 「8 | `account` | 最终 COA 科目快照。被学习的答案本身；最终权威仍在 Transaction Log。」`case_log/01_memory_intent.md:39`；「9 | `hst_gst_treatment` | 税务处理方式快照。答案的一部分；最终权威仍在 Transaction Log。」`case_log/01_memory_intent.md:40`
- 「12 | `confirmed_by` | ……具体人员和完整审计留痕归 Transaction Log。」`case_log/01_memory_intent.md:43`
- 「`Transaction Log`：每笔交易 final outcome、review trace 和 audit trail 的 source of truth。」`case_log/01_memory_intent.md:89`
- 「Case Log 是 entity-indexed reusable-precedent layer，不是 Transaction Log 的 entity 副本。」`case_log/01_memory_intent.md:119`
- 「Case Log 必须能引用 `transaction_log_ref` 或等价 finalization proof；它不能替代或重写 Transaction Log。」`case_log/01_memory_intent.md:122`
- 「Transaction Log 是 final 交易事实、完整 processing path、修改历史和 audit trail 的 source of truth；Case Log 只保存最新可学习状态。」`case_log/01_memory_intent.md:123`
- 【情况 X/Y 落点原文】「一笔交易可以在 entity 未解析的情况下被 accountant 完成分类并 finalize；这类交易只 finalize 到 Transaction Log，不进入 Case Log（无 entity_id 索引键），也不创建 Entity Log 记录。」`case_log/01_memory_intent.md:126`
- 【情况 Y 身份缺口标记落点原文】「这类交易不得催生以 description / 类别为索引的学习记录；其身份缺口（情况 Y）是挂在 Transaction Log 上的不阻塞复查线索（标记落点初步倾向 Transaction Log，未冻结），不是 candidate entity，也不是 Case Log 可引用的 durable identity handle。」`case_log/01_memory_intent.md:127`
- 边界表 · Transaction Log 行：「Transaction Log 保存 final 交易事实、完整 processing path、修改历史与 audit trail，是审计 source of truth。Case Log 只引用 `transaction_log_ref` / finalization proof，只保存最新可学习状态，不存修改历史、不存完整 processing path、不替代审计记录。entity 未解析即完成分类的交易只 finalize 到 Transaction Log，不进入 Case Log。 |（缺口栏）Case Log 与 Transaction Log 的 exact trigger order、writer、多 log finalization、correction / reversal / split 后的执行机制，以及身份缺口标记落点（初步倾向 Transaction Log）未冻结。」`case_log/02_authority_lifecycle_and_boundaries.md:110`
- finalization proof 边界表行：「finalization proof | `Transaction Log` 或等价 transaction finalization source | Case Log 必须引用完成证明……Case Log 自己不能替代 Transaction Log，也不能重写 final outcome、audit trail 或修改历史。」`case_log/02_authority_lifecycle_and_boundaries.md:11`
- 冲突 authority 顺序：「如果 Case Log 与 Transaction Log / finalization proof 冲突：authority 顺序：Transaction Log 或等价 finalization source 对 final outcome 和 audit trail 优先。」`case_log/02_authority_lifecycle_and_boundaries.md:119-121`
- boundary note：「`Transaction Log` 保存每一笔进入系统并完成处理的交易审计记录。」「`Case Log` 只保存从已完成交易中抽取出的可学习案例。它必须能引用 `transaction_log_ref`……但不能替代或重写 `Transaction Log`。」「所有交易完成过程和最终审计轨迹属于 `Transaction Log`。」`case_log/case_log_boundary_note.md:38,40,44`
- 00_index：「按 entity 查询全部历史交易属于 Transaction Log 的 entity 索引查询能力。」「unknown entity 交易即使被 accountant 完成分类并 finalize，也只进入 `Transaction Log`，不写 `Case Log` / `Entity Log`，且不得创建以 description / 类别为索引的学习记录。」`case_log/00_index.md:19,48`

A 在此接缝锁死的「存储边界划分」（什么归 Transaction Log vs 什么归 Case Log）：
- 归 Transaction Log：每笔交易 final outcome、完整 processing path、修改历史、audit trail、review trace、最终 COA / HST-GST 的最终权威、`confirmed_by` 的具体人员与完整审计留痕、entity 未解析交易（情况 X / Y）的 finalization、情况 Y 身份缺口的不阻塞复查线索（初步倾向）、按 entity 查询全部历史交易的 entity 索引能力。
- 归 Case Log（不归 Transaction Log）：entity-indexed reusable-precedent、最新可学习状态快照；Case Log 通过 `transaction_log_ref` 引 Transaction Log，不存修改历史 / 完整 processing path / 审计记录。
- authority 顺序：Transaction Log（或等价 finalization source）对 final outcome 和 audit trail 优先。

△缺口（A 明示未冻结）：
- Case Log 与 Transaction Log 的 **exact trigger order / writer / 多 log 统一 finalization** → A 明示未冻结。`case_log/00_index.md:60`、`case_log/01_memory_intent.md:143`、`case_log/02_authority_lifecycle_and_boundaries.md:95,99`、`缺口地图.md:77`
- correction / reversal / split 后对 Transaction Log 的执行机制 → A 明示未冻结。`case_log/02_authority_lifecycle_and_boundaries.md:110`
- 情况 Y 身份缺口标记落点（「初步倾向 Transaction Log，未冻结」）→ A 明示未冻结。`case_log/01_memory_intent.md:127`、`case_log/02_authority_lifecycle_and_boundaries.md:110`
- 多 log 统一写入机制如何同时处理 Entity Log / Case Log / Transaction Log / Intervention Log → A 明示未冻结。`case_log/02_authority_lifecycle_and_boundaries.md:99`

---

## 接缝 6 — Entity Log ↔ Transaction Log（存储边界 + runtime 隔离）

A 原文（Entity Log 文档逐项摘出）：
- 「`Transaction Log` 是 audit-facing final record，不参与 runtime decision。」`entity_log/01_memory_intent.md:19`
- 「COA account、HST / GST treatment、journal entry、final transaction outcome、transaction processing path：归 Transaction Log 或对应运行节点输出；Entity Log 不保存交易处理结果。」`entity_log/01_memory_intent.md:66`
- 「逐笔交易历史与完整 audit trail：归 Transaction Log；Entity Log 不得变成按 entity 索引的流水账。」`entity_log/01_memory_intent.md:69`
- 「`Transaction Log`：每笔交易最终结果和 audit trail 的 source of truth。」`entity_log/01_memory_intent.md:78`
- 「`Transaction Log` 不能作为 runtime identity source 或 learning source。」`entity_log/01_memory_intent.md:112`、`entity_log/00_index.md:42`
- 「`Entity Log` 独立于 `Case Log` 和 `Transaction Log`。」`entity_log/00_index.md:28`
- 边界表：「Transaction Log | 保存 final transaction audit record；不参与 runtime identity decision | （缺口栏）reprocessing / correction 如何影响 entity risk candidate 尚未冻结。」`entity_log/02_authority_lifecycle_and_boundaries.md:132`
- entity identity 行「绝不」列含「Transaction Log outcome」（即 entity identity authority 不来自 Transaction Log outcome）。`entity_log/02_authority_lifecycle_and_boundaries.md:9`

A 锁死的存储边界：
- 归 Transaction Log（不归 Entity Log）：COA / HST-GST / journal entry / final transaction outcome / transaction processing path / 逐笔交易历史 / 完整 audit trail。
- 边界：Entity Log 独立于 Transaction Log；Transaction Log **不**作 runtime identity source / learning source；Entity Log 不变成按 entity 索引的流水账（流水归 Transaction Log）。

△缺口（A 明示未冻结）：
- reprocessing / correction 如何影响 entity risk candidate → A 明示未冻结。`entity_log/02_authority_lifecycle_and_boundaries.md:132`

---

## 接缝 7 — Alias Log ↔ Transaction Log

A 原文（Alias Log 文档逐项摘出）：
- 「Transaction Log 是最终审计记录，不参与 runtime identity decision。」`alias_log/01_memory_intent.md:19`
- 「`Transaction Log`：final transaction audit record。」`alias_log/01_memory_intent.md:74`
- 边界表：「Transaction Log | Transaction Log 保存 final transaction audit record；不是 runtime Alias authority | （缺口栏）transaction correction 如何影响 Alias review / mutation 尚未冻结。」`alias_log/02_authority_lifecycle_and_boundaries.md:125`

A 锁死的边界：Transaction Log 是 final transaction audit record，**不是** runtime Alias authority。

△缺口（A 明示未冻结）：
- transaction correction 如何影响 Alias review / mutation → A 明示未冻结。`alias_log/02_authority_lifecycle_and_boundaries.md:125`

---

## 接缝 8 — Entity Resolution → Transaction Log

A 原文（Entity Resolution 文档逐项摘出）：
- 「ER 绝不：写入 `Transaction Log`（交易审计日志）。」`entity_resolution_node/01_functional_intent.md:57`、`entity_resolution_node/02_logic_and_boundaries.md:120`
- 「当前任务是 onboarding initialization、review approval、governance approval 或 transaction logging（交易审计记录）（时…）」`entity_resolution_node/02_logic_and_boundaries.md:17`
- 「该 publication 不代表 Alias、Rule、automation policy、Case Log 或 Transaction Log 已经写入。」`entity_resolution_node/02_logic_and_boundaries.md:61,155`
- 「`Transaction Log`（交易审计日志）。」（列于不写清单）`entity_resolution_node/02_logic_and_boundaries.md:77`
- Legacy 表：「Transaction Log 不参与 runtime decision | 旧系统 Transaction Log 边界 | 身份识别必须基于 current evidence 和 active authority，不能从最终历史审计记录中反向学习未治理结论。」`entity_resolution_node/02_logic_and_boundaries.md:282`

A 锁死的边界：ER **绝不写** Transaction Log；stable entity publication 不代表 Transaction Log 已写入；身份识别不能从 Transaction Log（最终历史审计记录）反向学习。

△缺口：此接缝 A 仅声明 ER 的「不写 / 不读」隔离边界，未对 Transaction Log 自身存什么 / 何时写补充约束（ER 侧无新增正向契约）。

---

## 9. 未落文邻居的 open boundary（A 提到但邻居无正式草案，绝不拿 B 填）

以下接缝在 A 桶里被多处当下游 / 旁挂引用，但对方节点 / log **尚无 BK_Copilot 正式草案**，故只能挂 open boundary，Transaction Log 与它们的契约整体未定：
- **JE Generation Node**：A 锁「JE result / journal_entry 留痕」语义指向 Transaction Log（CJ 02:182「不代表 JE 已生成」；Coordinator 02:129「不代表……Transaction Log 已写入」），但 JE↔Transaction Log 的 exact handoff 未落文。`case_judgment_node/00_index.md:44`、`coordinator_node/00_index.md:32`
- **Intervention Log**：A 锁「交互事实 → Intervention Log，结果审计不挂 Intervention Log → Transaction Log」分界（Coordinator 02:226），但 Intervention Log 正式 spec 未落文。
- **Review Node**：A 锁 review trace 归 Transaction Log audit trail（Case Log 01:89），但 Review Node 是否覆盖所有 final logging 前交易等未落文（与 Coordinator 同列 Case Log §3 finalization path）。`缺口地图.md:184`
- **Case Memory Update Node**：A 锁其「在交易 finalized 后凭 `transaction_log_ref` 处理 Case Log 写入」（CJ 01:80、02:184），但其 exact 写入机制未落文。`case_judgment_node/02_logic_and_boundaries.md:184`
- **Governance / Governance Review**、**Profile / Structural Match**、**Knowledge Summary / Knowledge Compilation Node**、**Evidence Log**、**interaction_agent**、**Post-Batch Lint**：A 仅旁挂引用（reasoning 供 governance 读、Evidence Log refs 等），无正式草案。`case_judgment_node/00_index.md:44`、`coordinator_node/00_index.md:27`

---

## 10. 已锁事实交叉（来自 00_rules.md / 已锁文档，约束 Transaction Log 不得越界）

- Transaction Log 仍是圈外 / 未正式落文对象，只能通过 `缺口地图.md` 和相关主题入口追踪接缝。`当前任务状态.md`（见 `00_rules.md:64`）
- 多 log finalization 顺序（Evidence / Transaction / Case / Rule / JE）属【L4/seam】，Evidence Log 尚未正式审计。`缺口地图.md`（见 `00_rules.md:72`）
- 清楚区分 evidence / memory / judgment / governance / audit records；不要把 AI reasoning 文本变成可复用 authority。`AGENTS.md:操作规则`（见 `00_rules.md:68-69`）
- `transaction_id` 铸号并入 Evidence Intake 第 9 步；跨导入复用 / 迟到证据接回 / durable registry 全部 DEFERRED【L4/seam】。`当前任务状态.md` / `缺口地图.md`（见 `00_rules.md:87-88`）
