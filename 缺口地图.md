缺口地图(每项已分类)
标签:【L2】本轮收口 ·【L3】留联合 L3 ·【L4/seam】机制,推给记录层、本轮只标归属不设计 ·【L2·外阻】性质 L2 但依赖圈外 store,挂起

Entity Resolution

stable 阈值(多强算 stable = stable/unknown 分界)→【L2】★本轮核心(注:这就是你之前 park 的那个开放问题;ER 的 L2 要"完整"绕不开它,新窗口需重新提出)
entity_resolution_output 字段 schema →【L3】
new_stable_entity provenance 字段 schema →【L3】
knowledge_summary 冲突修复流程 →【L4/seam】(冲突语义已定,修复是机制)
Entity Log

entity_record / provenance 字段 schema →【L3】
alias_record / Alias 库技术形态 →【L4】
automation_policy 与 Governance Log 的 projection 边界 →【L2·外阻】(依赖 Governance)
merge/split 后 alias/case/rule 的迁移或阻断语义 →【L2】★(机制部分【L4】)
person entity 与 Profile 边界 →【L2·外阻】(依赖 Profile)
automation_policy 自动降级 mutation contract →【L4/seam】
Alias Log

确认 stable entity 时,surface text 是否默认触发 alias 写入(=未解决问题1)→ 决策【L2】★;执行【L4/seam】
normalization / equivalence 匹配策略、高度类似 alias 何时支持更强 match →【L2】★(具体规则【L3】)
Alias 冲突 / 多 entity 竞争 / merge-split 迁移语义 →【L2】★(机制【L4】)
Alias 对下游的支持范围 →【L2】(其中 Rule Match 在圈内;其余下游挂起)
alias_record 字段 schema →【L3】;Alias 库技术形态、与 Entity Log 存储边界 →【L4】
写入责任/审批/统一 write 机制 →【L4/seam】(其中"谁有权确认 alias"=【L2】)
Alias mutation 与 Governance audit 边界 →【L2·外阻】
Case Log

write eligibility(哪些完成交易写成 case)→【L2】★本轮核心
completed/corrected/reversed/split 的 supersession 语义 →【L2】★(机制【L4】)
record schema / field / enum / validation →【L3】
exact writer、与 Transaction Log 的 trigger order、多 log 统一写入 →【L4/seam】
case-derived rule/automation/entity risk candidate 的 governance contract →【L2·外阻】(Governance;rule 部分边缘连 Rule Match)
与 Knowledge Summary 冲突修复 →【L4/seam】(Knowledge 圈外)
Rule Match — greenfield,整份 L1 + L2 待写(从 Rule Match临时文档.md 起草:entity-level / pattern-level rule scope、Alias 在上游、多分类 entity 走更窄 scope、自动化原则)。依赖 ER 的 stable entity 输出先锁定。

